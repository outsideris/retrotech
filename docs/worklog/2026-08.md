# 작업 로그 — 2026-08

## 2026-08-08

### 에디터: 대본 링크에서 utm_source=chatgpt.com 항상 제거

- Summary: 대본 임포트의 링크 추출(`ExtractLinks`)에서 `utm_source=chatgpt.com` 쿼리 파라미터를 항상 제거. 그 외 URL(다른 파라미터·앵커 포함)은 대본 그대로 사용한다.
- Context: 발행본 2h 와 도구 생성 초안의 레퍼런스 전수 비교(159 vs 165, 공통 152)에서 초안 URL 3개에 ChatGPT 로 대본을 쓸 때 묻은 `?utm_source=chatgpt.com` 이 남는 것을 확인 — 수동 등록 때는 손으로 제거하던 것. 사용자 결정: 이 파라미터만 자동 제거, 나머지 URL 은 원본 유지.
- Changes: `internal/editor/assist/links.go` — `stripChatGPTUTM`(중간/끝 위치 2패스 정규식, 다른 파라미터·프래그먼트 보존), dedupe 전에 적용해 클린 URL 과 utm 변형이 한 항목으로 합쳐짐(2h 대본에 실제로 있던 중복).
- Tests: `go vet ./...`·`go test ./...` green.
- Tests Added: `links_test.go` — 단독/뒤/앞 파라미터·프래그먼트 유지·다른 utm(newsletter 등) 보존 5케이스 + 클린/utm 변형 dedupe 1케이스.
- Quality Gate: 컴파일/정적검사·테스트 확인. 에디터 전용 변경(사이트 빌드 무관, Lighthouse 면제 조항).
- Verify: 단위 테스트로 검증(순수 문자열 처리라 E2E 불필요).
- Docs: DESIGN.md(URL 은 대본 그대로 + 유일한 예외 명시), plan/episode-editor-app.md, TESTS.md.
- Next: 없음 — 재패키징·재설치 완료.

## 2026-08-07

### [버그] 대본 임포트마다 초안이 하나씩 쌓이던 문제 — find-or-create 로 수정

- Summary: 같은 대본을 다시 임포트해도 항상 새 초안이 생겨 동일 에피소드 초안이 계속 쌓이던 문제를 수정. `POST /drafts` 를 find-or-create(에피소드 id 기준)로 확장해 재임포트는 기존 초안을 갱신하게 하고, 분석 진행 중 중복 임포트를 클라이언트에서 차단했다.
- Context: 사용자 보고 — "분석이 완료되면 초안이 하나씩 더 생긴다". 포렌식: `content/drafts/` 에 id=2h 초안 3개(00:31/01:07/01:09), `~/.claude/projects/`(앱 번들 cwd)의 CLI 세션 4개 중 3개가 **바이트 동일 프롬프트**(같은 대본) — 00:47 시작 건은 정확히 3분 뒤 kill(구버전 타임아웃, "signal: killed" 보고 건), 01:03·01:07:44 두 건은 각각 완료 시점에 초안 생성. 임포트가 무조건 `newDraft()` 로 새 초안을 만들어, 재분석(재시도·중복 트리거 포함)마다 초안이 하나씩 늘어나는 구조가 근본 원인.
- Changes:
  - `internal/editor/drafts.go`: `FindByEpisodeID` — 폼 id 가 일치하는 가장 최근 초안 반환(손상 초안은 건너뜀).
  - `internal/editor/editor.go`: `handleDraftCreate` 가 선택적 body `{"id"}` 를 받아 동일 id 초안이 있으면 200 으로 재사용, 없으면 기존대로 201 생성(무 body 호출은 기존 동작 그대로).
  - `internal/editor/assets/app.js`: `newDraft(episodeID)` 로 id 전달, `applyScriptMeta` 가 분석된 id 로 호출(재임포트 = 기존 초안 갱신), `btn-new` 리스너를 `() => newDraft()` 로 래핑(클릭 이벤트가 id 인자로 새던 잠재 버그 겸사 수정), `state.importing` 가드로 분석 중 재드롭 차단.
- Tests: `go vet ./...`·`go test ./...` green.
- Tests Added: `drafts_test.go` `TestDraftFindByEpisodeID`(일치·미일치·빈 id), `editor_test.go` `TestDraftCreateFindOrCreateByID`(동일 id 200 재사용·초안 개수 불변·다른 id 201).
- Quality Gate: 컴파일/정적검사·테스트 확인. 에디터 전용 변경(사이트 빌드 무관, Lighthouse 면제 조항).
- Verify: 새 바이너리 사이드카에 `POST /drafts {"id":"2h"}` → 기존 초안 slug(draft-20260807-010923) 재사용·초안 개수 불변 확인. (검증 중 구버전 사이드카에 잘못 보낸 호출로 생긴 빈 초안 1개는 즉시 삭제.)
- Docs: plan/episode-editor-app.md(임포트 초안 재사용 규칙), TESTS.md(초안 find-or-create 행).
- Next: 없음 — 재패키징·재설치 완료. 사용자 저장소에 남은 중복 2h 초안 2개(00:31·01:07)는 사용자가 UI 에서 삭제하면 됨(데이터라 임의 삭제하지 않음).

### [버그] 대본 임포트 "claude: signal: killed" — 3분 타임아웃이 실대본에서 초과

- Summary: 실제 대본을 드롭하면 "claude: signal: killed" 로 실패하던 문제 수정. 분석 제한 시간을 3분 → 10분으로 늘리고, 초과 시 원인을 알 수 있는 504 메시지로 변환하며, 분석 중 경과 시간을 UI 에 표시한다.
- Context: 사용자 보고. 원인 — `handleAssistAnalyze` 의 `context.WithTimeout(…, 3*time.Minute)` 이 만료되면 `exec.CommandContext` 가 CLI 를 SIGKILL 하고, 그 exec 에러("signal: killed")가 그대로 UI 에 노출됐다. 짧은 샘플(5링크, 12초)은 통과했지만 실제 대본은 전체 스크립트 + 수십 개 링크 제목 작업이라 3분을 초과.
- Changes:
  - `internal/editor/editor.go`: `assistAnalyzeTimeout`(10분)·`assistRunTimeout`(3분) 상수 분리, `writeAssistError` — ctx 데드라인 초과면 504 + "AI 응답이 제한 시간(N분)을 넘어 중단되었습니다…"(모델/effort 조정 안내), ErrUnavailable 503, 기타 502(기존 매핑 유지).
  - `internal/editor/assets/app.js`: `importScript` 가 분석 중 1초 간격으로 경과 시간(`N분 N초`) 표시 — 수 분 걸리는 분석이 멈춘 것처럼 보이지 않게.
- Tests: `go vet ./...`·`go test ./...` green.
- Tests Added: `editor_test.go` `TestWriteAssistError` — 타임아웃 → 504·한국어 안내·"signal: killed" 미노출, ErrUnavailable → 503, 기타 → 502.
- Quality Gate: 컴파일/정적검사·테스트 확인. 에디터 전용 변경(사이트 빌드 무관, Lighthouse 면제 조항).
- Verify: 재현은 실 CLI 3분+ 호출이라 생략 — SIGKILL 경로는 exec.CommandContext 의 문서화된 동작이고 에러 문자열("claude: signal: killed")이 `runErr` 출력 형식과 정확히 일치함을 코드로 확인. 매핑은 단위 테스트로 검증.
- Docs: plan/episode-editor-app.md(타임아웃·에러 변환), TESTS.md(editor_test 행).
- Next: 없음 — 재패키징·재설치까지 완료. 10분으로도 부족한 초대형 대본이 실제로 나오면 분석을 비동기 잡(폴링)으로 전환 검토.

### 에디터: 대본 임포트 시 링크 전부를 레퍼런스로 자동 정리 (+제목 규칙 확립)

- Summary: 대본(.md)을 Assist 드롭존에 놓으면 대본의 모든 링크를 문서 순서대로 레퍼런스 행으로 채우도록 구현. 제목은 앵커 텍스트가 아니라 링크 대상 페이지의 실제 제목을 쓰는 규칙을 기존 회차 데이터에서 도출해 프롬프트에 내장했다.
- Context: 사용자 요청 — "대본에서는 문맥에 링크가 걸려있지만 레퍼런스는 해당 사이트의 타이틀 위주로, 이미 퍼블리시한 레퍼런스 데이터를 참고해 규칙을 세우고 이후에도 따르게". 기존 24편 1,495개 레퍼런스를 분석해 규칙 도출(최신 2g·2h 관례 우선): "X: Wikipedia", "<이름>: LinkedIn/X/Homepage", "<연도>년의 <사이트>"(archive.org), 사이트 홈은 이름만, 글/논문/영상은 원제 그대로(원문 언어 유지), 모호하면 "<저자>가 쓴 <제목>".
- Changes:
  - `internal/editor/assist/links.go`(신규): `ExtractLinks` — 마크다운 링크(이미지 제외)+맨 URL 을 문서 순서·URL 중복 제거로 결정적 추출(모델에 맡기지 않아 누락 원천 차단). 괄호 포함 URL(Wikipedia)·후행 문장부호 처리.
  - `internal/editor/assist/analyze.go`: `ScriptMeta.References` 추가, `refTitleRules` 상수(제목 규칙 — 프롬프트 내장으로 이후 임포트 전부에 적용), `analyzePrompt` 가 추출 링크 목록을 명시하고 제목만 요구, `reconcileRefs` 가 응답을 추출 목록과 대조(누락 → 앵커/URL 폴백, 목록 밖 URL 폐기 — 결과는 항상 "링크 전부·문서 순서·정확히 한 번").
  - `internal/editor/editor.go`: `/api/assist/analyze` 응답에 `references` 포함.
  - `internal/editor/assets/app.js`·`index.html`: `applyScriptMeta` 가 레퍼런스 행 렌더 후 자동 저장, 드롭존 문구에 레퍼런스 추가.
- Tests: `go vet ./...`·`go test ./...` green.
- Tests Added: `assist/links_test.go`(추출 4케이스 — 혼합 순서·중복·이미지·문장부호), `assist/analyze_test.go`(+references 파싱, `reconcileRefs` 폴백/폐기/순서, `analyzePrompt` URL 전수·규칙 포함·링크 없으면 미요청).
- Quality Gate: 컴파일/정적검사·테스트 확인. 사이트 빌드 무관(에디터 전용) — `go run ./cmd/build` 는 이전 커밋 후 무변경이나 재확인. Lighthouse 면제(에디터 앱 조항).
- Verify: 실제 claude CLI 로 E2E 1회(임시 사이드카 + 샘플 대본 5링크) — 전 링크가 순서대로 반환되고 규칙 준수 확인: "Linus Torvalds: Wikipedia", "BitKeeper"(홈), LWN 글 원제, 앵커 "여기" → 내용 기반 제목, "2005년의 Git 홈페이지"(archive).
- Docs: DESIGN.md(레퍼런스 제목 규칙 — 사람용 기록), plan/episode-editor-app.md(추출·대조 설계), TESTS.md(links 행 추가·analyze 행 갱신).
- Next: 없음 — 재패키징 후 `/Applications/RetroTech Editor.app` 교체·재실행 완료(49218 에서 갱신 UI 서빙 확인).

## 2026-08-02

### 에디터: description 을 도입부(intro)에서 자동 파생 — 중복 입력 제거

- Summary: 프론트매터 `description` 과 본문 도입부가 전 회차(24편)에서 같은 내용임을 확인하고, 에디터에서 도입부 하나만 편집하면 서버가 저장 시 description 을 파생(마크다운 링크 제거)하도록 바꿨다. 구조화 모드에서 description 입력란을 숨겼다.
- Context: 사용자 관찰 — "메타데이터의 설명과 intro 는 같은 내용일 것". 전수 비교 결과 24/24 동일(유일한 실질 차이는 옛 회차 4편의 도입부 마크다운 링크와 줄바꿈 위치). AI 대본 분석이 description 만 채우고 intro 를 비워두는 문제도 함께 해소.
- Changes:
  - `internal/editor/derive.go`(신규): `deriveDescription` — 링크/이미지 → 텍스트(1단계 중첩 괄호 URL 허용), trailing newline 1개(기존 블록 스칼라 clip 규약), 멱등.
  - `internal/editor/store.go`: `Create` 는 구조화 폼의 description 을 항상 파생으로 덮어씀. `Update` 는 **intro 가 바뀐 경우에만** 재파생하고 미변경 시 저장된 바이트 보존 — 옛 회차(description 줄바꿈이 intro 와 다름)를 무관한 필드 수정으로 저장해도 피드가 안 바뀌게(피드 골든 보호).
  - `internal/editor/drafts.go`: `Save`(자동저장)가 구조화 초안의 description 을 항상 파생. 발행은 `Store.Create` 경유로 자동 커버.
  - `internal/editor/editor.go`: 생성/수정/초안저장 핸들러가 저장 후 재조회한 폼을 응답(파생 값 반영).
  - `internal/editor/assets/index.html`·`app.js`: 구조화 모드에서 description 필드 숨김(원문 마크다운 모드에서만 노출), 도입부에 자동 생성 힌트 추가, 대본 분석 결과가 `f-intro` 를 채우도록 변경, 드롭존 문구 갱신.
- Tests: `go build ./...`·`go vet ./...`·`go test ./...` 전부 green(에디터 라운드트립 23편 전수·피드 골든 포함). `go run ./cmd/build` 성공(24편, dist 산출 확인).
- Tests Added: `derive_test.go`(파생 규칙 9케이스), `store_test.go`(+Create 파생·Update 레거시 보존/재파생), `drafts_test.go`(+Save 파생, 발행 파생 단언), `editor_test.go`(+HTTP 파생 응답·낡은 클라이언트 description 오염 방지).
- Quality Gate: 컴파일/정적검사·테스트·빌드 확인. RSS/정적 산출물 — 콘텐츠 무변경이라 기존과 동일(피드 골든 green 으로 확인). Lighthouse — 사이트 마크업 무변경(에디터 전용)으로 면제(QUALITY_GATE.md 에디터 앱 조항). GUI 는 `go run ./cmd/app` + 브라우저로 수동 확인(설명란 숨김·힌트·드롭존 문구 — 데스크톱 앱 반영은 재패키징 필요).
- Verify: 임시 사이드카(63992 포트)로 실제 UI 확인 — 기존 2h 회차에서 설명란 미노출·도입부 힌트 표시. 참고: 49218 은 실행 중인 데스크톱 앱(구버전)이 점유 중이었음.
- Docs: DESIGN.md(에디터 원칙 bullet), ARCHITECURE.md(derive.go·Update 보존 규칙), plan/episode-editor-app.md(파생 설계), TESTS.md(4개 행 갱신+derive 행 추가).
- Next: 없음 — 재패키징(`npm run dist`) 후 `/Applications/RetroTech Editor.app` 교체·재실행까지 완료(새 사이드카가 49218 에서 갱신된 UI 서빙 확인).

## 2026-08-01

### [버그] 에디터 앱: 백엔드 서버가 죽으면 좀비 UI 가 되던 문제 — 자동 재시작 추가 16:01

- Summary: 데스크톱 앱에서 왼쪽 목록을 클릭해도 메인 화면에 에피소드가 안 나타나던 문제의 원인을 규명하고, Electron 셸에 Go 사이드카 감시·자동 재시작을 추가했다.
- Context: 사용자 보고 — "새로 띄우면 잘 되는데 다른 앱을 쓰다가 나중에 돌아오면 안 된다". 조사 결과 앱(Electron)은 떠 있는데 백엔드 `editor-server` 프로세스만 죽어 있었다.
- 원인(포렌식): 2026-07-31 02:42 KST, blog.outsider.ne.kr 프로젝트의 다른 에이전트 세션이 Outsider Writer 앱을 재설치하며 `pkill -f "Resources/editor-server"` 를 실행 — 이 패턴은 **같은 바이너리 이름을 쓰는 RetroTech Editor 의 사이드카까지 매치**되어 함께 죽였다(세션 트랜스크립트에서 명령·시각 확인, jetsam 스냅샷으로 사망 시점 교차 검증). 기존 `desktop/main.js` 는 서버 시작 후의 exit 를 전혀 처리하지 않아(시작 전 실패만 reject) 창은 살아 있지만 모든 API fetch 가 실패하는 좀비 상태가 됐고, 실패 메시지도 폼 상태줄에만 표시돼 사실상 "클릭해도 아무 일도 안 일어남"으로 보였다.
- Changes:
  - `desktop/main.js`: `superviseServer`/`restartServer` 추가 — 실행 중 서버가 죽으면 자동 respawn. 고정 포트(49218) 재바인드가 일반 경로라 페이지(미저장 폼 상태 포함)는 그대로 살아나고, 포트가 바뀐 경우에만 창을 새 URL 로 리로드. `quitting` 플래그로 종료 중 재시작 억제. `changeRepoFolder` 는 `app.exit(0)` 가 quit 이벤트를 건너뛰므로 서버를 명시적으로 kill 하도록 보강(고아 서버 잠재 누수 수정).
  - `desktop/restart-policy.js` (신규): 크래시 루프 가드 — 10초 이상 살았으면 카운터 리셋, 연속 quick failure 3회 초과 시 재시작 포기(에러 다이얼로그 후 종료). 순수 모듈·클록 주입으로 Electron 없이 테스트 가능.
  - `desktop/package.json`: `npm test` (`node --test`) 스크립트 추가.
  - `internal/editor/assets/app.js`: `request()` 가 네트워크 수준 실패 시 **GET 에 한해** 500ms 후 1회 재시도(서버 재시작 찰나 커버). 쓰기(POST/PUT/DELETE)는 중복 실행 위험이 있어 재시도하지 않음.
- Tests: `cd desktop && npm test` 5 passed / `go build ./...` / `go vet ./...` / `go test ./...` 전부 green.
- Tests Added: `desktop/restart-policy.test.js` — 안정 사망은 항상 재시작, 크래시 루프 3회 후 차단, 안정 구동 시 카운터 리셋, 준비 실패 spawn 도 quick failure 로 계수, **느린(≥10s) 시작 실패도 카운터 미리셋**(리뷰 발견 회귀 가드) 5케이스.
- Quality Gate: go build·vet·test green. `go run ./cmd/build` — 사이트 빌드 무관 변경이나 실행 확인. Lighthouse — 에디터 전용 변경으로 면제(QUALITY_GATE.md 42행 에디터 앱 조항). GUI 동작은 재빌드·재설치 후 수동 확인.
- Verify: 적대적 리뷰 워크플로(2 lens 리뷰 → 반박 검증, 7 agents) 가 실결함 2건 확인 → 즉시 수정(1회차). ① `startServer` 에 child `error` 핸들러 부재 — spawn 실패(ENOENT 등) 시 promise 미settle 로 재시작 루프 영구 정지 + main process uncaught exception(검증 에이전트가 Node 실증 재현) → `proc.on("error")` 로 reject/로깅. ② 크래시 루프 가드가 spawn 시각 기준 리셋이라 10초 넘게 걸려 실패하는 서버(예: 파일시스템 멈춤)는 무한 재시작 → 정책을 `started()/ready()/shouldRestart()` 로 바꿔 **준비 완료 후 생존 시간** 기준으로만 리셋. 수정 후 검증 패스(2 agents) 모두 ok — 전 경로 단일 settle·무한 루프 종결성 추적 확인.
- 설치 중 발견한 3번째 버그: electron-builder `files` 화이트리스트에 `restart-policy.js` 누락 — 패키징 앱이 시작 시 `require` MODULE_NOT_FOUND 로 `app.setName` 전에 죽어 userData 가 기본값(`retrotech-editor`)으로 갈리고 창/서버 없이 에러 상태로 대기. `files` 에 추가 후 재패키징으로 해결. **교훈: `desktop/` 에 새 JS 파일을 추가하면 반드시 `package.json` build.files 에도 추가**(ARCHITECURE.md 에 명시).
- E2E 검증(설치된 앱): 재설치·재실행 후 원 사고 재현 — `pkill -f "RetroTech Editor.app/Contents/Resources/editor-server"` (앱 경로 스코프) → 3초 내 자동 respawn(PID 98670→98821)·동일 포트(49218) 재바인드·`/_write/api/episodes` 24건 정상 응답 확인.
- Docs: TESTS.md(데스크톱 JS 테스트 추가), QUALITY_GATE.md(명령 표에 npm test), ARCHITECURE.md(감시·재시작 구조).
- Next: blog.outsider.ne.kr 쪽에 "이름 기반 pkill 금지(앱 경로로 스코프)" 가이드 반영 — 별도 태스크로 분리.
### 팟캐스트 챕터(타임스탬프) 지원 — 프론트매터 → 피드

- Summary: 에피소드 프론트매터에 `chapters:`(start/title)를 선언하면 Podcasting 2.0 chapters JSON(`episodes/<id>.chapters.json`) + 피드 `<podcast:chapters>` 태그 + description `MM:SS 제목` 타임스탬프 줄로 반영되도록 구현. 온사이트 재생기 없이(기존 DESIGN 결정 유지) 팟캐스트 앱에서 구간 이동이 동작한다.
- Context: 사용자 요청 "소제목 등 특정 구간으로 이동하는 타임스탬프". 온사이트 플레이어(방식2) 대신 청취가 실제로 일어나는 플랫폼에서 동작하는 피드 경로(방식1)를 채택. 대안 비교는 [plan/podcast-chapters.md](../plan/podcast-chapters.md).
- Changes:
  - `internal/parser/parser.go`: `Chapter` 타입(`Start`/`Title`) + `StartSeconds()`("MM:SS"/"HH:MM:SS", 선두 필드 60 이상 허용), `Frontmatter.Chapters`, `LoadEpisode` 에서 불량 챕터 즉시 에러(파일명 포함).
  - `internal/builder/chapters.go`(신규): `BuildChaptersJSON`(Podcasting 2.0 v1.2.0), `ChaptersRelPath`.
  - `internal/builder/feed.go`: 챕터 에피소드가 있을 때만 `xmlns:podcast` 선언(그 전까지 피드 바이트 불변), 아이템 `<podcast:chapters>`, `feedDescription` 에 챕터 줄 병기.
  - `cmd/build/main.go`: 챕터 선언 에피소드의 `episodes/<id>.chapters.json` 출력.
- Tests: `go build ./...`·`go vet ./...`·`go test ./...` 통과(피드 골든 포함 — 챕터 미선언 상태의 피드 무변화 확인). `go run ./cmd/build` 성공. 임시로 2g에 챕터를 넣어 실빌드로 JSON/네임스페이스/description 병기를 확인 후 원복(dist 베이스라인 복원 확인).
- Tests Added: `internal/parser/parser_test.go`(챕터 파싱, `StartSeconds` 유효/무효 표, 불량 챕터 로드 거부), `internal/builder/chapters_test.go`(JSON 생성/빈 챕터 nil/에러/경로), `internal/builder/feed_test.go`(챕터 시 네임스페이스·태그·타임스탬프 줄, 미선언 시 부재).
- Quality Gate: 컴파일/정적검사·테스트·빌드·RSS 생성·정적 산출물 확인. Lighthouse CI 는 미실행 — 페이지 HTML/CSS 를 전혀 건드리지 않아(피드·신규 JSON만) 감사 대상 마크업이 변경 전과 동일하고, `a11y_perf_test.go` 불변식은 통과.
- Verify: 임시 챕터 데이터로 end-to-end(빌드 산출물) 확인 1회, 원복 후 베이스라인(챕터 JSON 없음·`xmlns:podcast` 0개) 재확인.
- Docs: ARCHITECURE(프론트매터 스키마·파이프라인·피드 챕터 경로), DESIGN(결정 표+챕터 원칙), TESTS, TODO(Phase 9), QUALITY_GATE(산출물 항목·검토 이력), plan/podcast-chapters.md(신규).
- Next: 회차별 실제 챕터 타임스탬프는 저자가 프론트매터에 입력(첫 챕터 `00:00` 권장). 필요 시 에피소드 페이지 정적 챕터 목차는 후속. **주의(머지 시 확인):** 에디터 앱은 아직 `chapters:` 를 몰라 챕터 회차를 에디터로 저장하면 유실됨 — TODO Phase 9 후속 항목 참고.
