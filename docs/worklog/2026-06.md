# 작업 로그 — 2026-06

## 2026-06-14

### Player.tsx 잔재 관련 문서 정리

- **Summary:** 옛 작업의 잔재였던 `components/Player.tsx` 를 사용자가 삭제함에 따라, 그 파일을 전제로 들어갔던 서술(빌드 차단 이슈, 인앱 플레이어 WIP 등)을 문서에서 제거하고 빌드 상태 기술을 정정했다.
- **Context:** 직전 커밋(c655078)은 Player.tsx 가 빌드를 깨뜨린다고 기록했으나, 해당 파일은 미사용 잔재로 사용자가 삭제. 이제 `npm run build` 가 정상 통과한다.
- **Changes:** ARCHITECURE/DESIGN/PERFORMANCE/TODO/QUALITY_GATE/TESTS/README/worklog 에서 Player.tsx·인앱 플레이어 관련 내용 제거. `howler` 는 "현재 미사용 의존성"으로만 남기고 제거 후보를 TODO Phase 4 에 정리.
- **Tests:** `npm run build` 성공 확인(정적 HTML 27p 익스포트).
- **Quality Gate:** `npm run build` 통과.
- **Docs:** 위 문서들 갱신.
- **Next:** docs/TODO.md Phase 1(이미지·CLS) 부터 개선 권장.

### 성능 검사 및 설계·구성 문서화

- **Summary:** 현재 구성으로 정적 사이트를 빌드해 성능을 측정하고, 코드를 다시 읽지 않고도 참고할 수 있도록 설계/구성/성능 문서 일습을 작성했다.
- **Context:** 사용자 요청 — "현재 구성에서 빌드된 사이트의 성능을 검사하고, 이후 작업 시 참고할 설계·구성 문서를 작성".
- **Changes (조사/측정, 코드 변경 없음):**
  - `npm run build` 로 정적 사이트를 빌드(통과)한 뒤 측정.
  - `dist/` 를 `python3 -m http.server` 로 서빙, Chrome DevTools 성능 트레이스 + Lighthouse 측정.
  - 빌드가 생성한 `public/feed.xml`(미추적·비-gitignore)은 세션 시작 상태로 되돌리기 위해 제거.
- **측정 결과(요약, 상세는 docs/PERFORMANCE.md):**
  - 정적 HTML 27p, `dist/` 3.7 MB. 홈 First Load JS ~104 kB.
  - 최대 이슈: `cover.svg` 402 KB(gzip 153 KB) 히어로, FontAwesome Pro 킷 풀로드(외부 ~9요청)+GitHub Sponsors iframe+GTM/GA 전 페이지 부하, 히어로 이미지 높이 미예약으로 Lighthouse 모바일 **CLS 0.25**.
  - Lighthouse(모바일): A11y 94 / BP 100 / SEO 100 / Agentic 25. `link-name`·`landmark-one-main` 실패.
  - `next/image`(akamai 로더)는 이 구성에서 최적화하지 않음(passthrough).
  - 한계: 로컬 서빙이라 TTFB≈1ms·무압축·무캐시 → 로딩 지표는 낙관적, 호스트 설정은 미검증.
- **Tests:** 자동화 테스트 없음(러너 부재). `npm run build` 로 타입체크/익스포트 확인(통과).
- **Tests Added:** 없음(이번 작업은 조사·문서화).
- **Quality Gate:** `npm run build` 성공.
- **Docs:** 신규 — `docs/ARCHITECURE.md`, `docs/DESIGN.md`, `docs/PERFORMANCE.md`, `docs/TODO.md`, `docs/QUALITY_GATE.md`, `docs/TESTS.md`, 본 worklog. `README.md` 보강.
- **Next:** docs/TODO.md Phase 1(이미지·CLS) 부터 개선 권장.
