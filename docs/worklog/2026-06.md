# 작업 로그 — 2026-06

## 2026-06-16

### 배포 게이트: cf-build.sh 에 테스트 게이트 추가

- **Summary:** Cloudflare 배포가 테스트를 통과해야만 진행되도록 `scripts/cf-build.sh` 에 `npm test` 게이트를 추가했다. 테스트(또는 빌드) 실패 → non-zero 종료 → Cloudflare 가 배포를 막고 직전 버전 유지 + ❌ 알림.
- **Context:** 사용자 요청. Cloudflare 자동 배포는 빌드 명령 종료 코드로 배포 여부를 정하므로, 빌드 명령(cf-build.sh)에서 빌드 전에 `npm test` 를 돌려 게이트로 삼음.
- **Changes:**
  - `scripts/cf-build.sh` — `npm test` → (통과 시) `npm run build`. `phase`(test/build)로 실패 단계를 알림 메시지에 표시. 어느 단계든 실패하면 non-zero 종료.
  - `vitest.config.ts` — `.claude/worktrees/*`(다른 세션 워크트리 사본)를 test `exclude` 에 추가. 워크트리에 있던 테스트 파일 사본 때문에 vitest 가 16→32개로 중복 실행하던 것을 수정(로컬 한정; CI/Cloudflare 의 깨끗한 체크아웃엔 없음).
- **Tests:** `npm test` → 2파일·16개(중복 제거 확인). `cf-build.sh` 정상 경로 → 테스트 16 통과 → 빌드 → exit 0. 실패 경로(테스트 실패 가정) → 빌드 생략·exit≠0·"test failed" 메시지 확인.
- **Quality Gate:** `npm run build` + `npm test` 통과.
- **Docs:** DEPLOYMENT.md(빌드 명령·동작·devDeps 조건), worklog.
- **Next:** 대시보드 빌드 명령이 `bash scripts/cf-build.sh` 인지 확인(아니면 게이트 미적용). 빌드 환경 devDeps 설치·Node 18+ 확인.
### 성능 — 홈/404 cover 이미지 preload (P2)

- **Summary:** LCP 요소인 `cover.svg` 를 **홈·404 페이지 head 에만** `<link rel="preload" as="image" fetchpriority="high">` 로 미리 받게 함. body 파싱 전에 발견돼 LCP load delay 단축. 에피소드 페이지는 cover 가 없어 제외(불필요한 프리로드 방지).
- **Changes:** `render.go`(`coverPreload` 상수 + 빌더별 전달), `render_layout.go`(`pageShell`/`headHTML` 에 페이지별 `headExtra` 파라미터 추가, viewport 직후 삽입).
- **검증:** `go test ./...` 통과. preload 가 index·404 에 1개, 에피소드/목록엔 0개 확인.

### 성능 — Sponsors iframe 지연 로드 (P1)

- **Summary:** 푸터의 GitHub Sponsors `<iframe>` 에 `loading="lazy"` 추가. 접힘 아래에 있어 초기 페이지 로드에서 github.com 요청을 미룬다(전 페이지 공통 비용 절감).
- **Changes:** `render_layout.go` footerHTML. `docs/TODO.md` Phase 2 항목 완료.
- **검증:** `go test ./...` 통과. dist iframe 에 `loading="lazy"` 확인.

### 접근성 — 다크모드 토글 키보드 작동 (A2)

- **Summary:** `role="button"` span 토글이 click 만 처리해 키보드(Enter/Space)로 작동하지 않던 것을, keydown 핸들러를 추가해 고침. 시각/구조 변경 없음.
- **Changes:** `render_assets.go` `darkModeToggleScript` — 토글 로직을 함수로 추출, `click` + `keydown`(Enter/Space, `preventDefault`) 바인딩.
- **검증:** 브라우저에서 토글에 포커스 후 Enter→다크 적용·localStorage `dark` 저장, Space→라이트 복귀 확인(evaluate_script).

### 접근성 — `<main>` 랜드마크 + 스킵 링크 (A1·A3)

- **Summary:** 페이지 셸의 `<article>` 에 `role="main"` + `id="content"` 부여(Lighthouse `landmark-one-main` 실패 해소 — 옛 사이트의 main 수정이 마이그레이션에서 회귀했던 것), `<body>` 첫머리에 "본문 바로가기" 스킵 링크 추가(포커스 시에만 표시).
- **Changes:** `render_layout.go`(pageShell·headHTML), `render_assets.go`(skipLinkStyle), `render_test.go`(회귀 테스트).
- **검증:** `go test ./...` 통과. dist 전 페이지에 `role="main"` 1개·`#content` 타깃 확인.

## 2026-06-16

### sitemap.xml 추가 (빌드 시 생성 → 새 에피소드 자동 반영)

- **Summary:** `sitemap.xml` 을 빌드 시 생성하도록 추가. 새 에피소드를 추가하고 빌드하면 사이트맵에 자동 반영된다.
- **Context:** 사용자 요청 — 사이트맵 추가 + 새 에피소드 시 갱신. 참고 `blog.outsider.ne.kr` 의 `encoding/xml` 사이트맵 패턴(랜딩 lastmod=최신 글 날짜) 차용. 피드와 달리 바이트 패리티 제약이 없어 `encoding/xml` 사용.
- **Changes:**
  - `internal/builder/sitemap.go` — `BuildSitemap`: 홈·/episodes·각 에피소드 URL(`urlset`/sitemaps.org 0.9). 에피소드 `lastmod`=발행일, 랜딩(홈·/episodes) `lastmod`=최신 에피소드 날짜(스캔으로 계산 → 정렬 무관). 404·feed 제외.
  - `internal/builder/sitemap_test.go` — 구조·URL 포함·404 제외·랜딩 lastmod·유효 XML.
  - `cmd/build/main.go` — feed 다음에 `dist/sitemap.xml` 생성. `siteURL` 상수로 추출(feed·sitemap 공유, SITE_URL 중복 일부 해소).
  - `public/robots.txt` — `Sitemap: …/sitemap.xml` 추가(크롤러 발견).
  - 문서: ARCHITECURE(빌드 파이프라인·산출물·sitemap 설명), QUALITY_GATE(산출물), TESTS(sitemap_test·누락됐던 render_test 추가).
- **검증:** `go vet`·`go test ./...` 통과. `go run ./cmd/build` → `dist/sitemap.xml` 25개 URL(홈+episodes+23), 랜딩 lastmod 2026-03-07(최신), 404 미포함, robots Sitemap 라인 확인.

### 1e 레퍼런스 링크 http→https (라이브와 일치)

- **Summary:** `content/episodes/1e.md` 의 "Prototype.js and Mochikit" 링크(`griddlenoise.blogspot.com`)를 `http://`→`https://` 로 변경. 전체 페이지 라이브 대조에서 유일하게 남아 있던 차이(소스 http vs 라이브 https) 해소.
- **Context:** 원본 `1e.mdx` 가 http 였고 변환 시 그대로 보존했으나(제 버그 아님), 사용자 요청으로 https 로 상향(더 안전·라이브 일치). 1e 의 다른 http 레퍼런스 51개(web.archive 등)는 라이브와 이미 일치하므로 미변경.
- **Changes:** `content/episodes/1e.md`(해당 1줄).
- **검증:** `go test ./...` 통과(본문 변경이라 피드 무관). 렌더된 `1e.html` 에 https 확인.

### Phase 7 정리 — dead Nextra CSS 규칙 제거

- **Summary:** 재사용 테마 CSS(`styles.css`)에서 RetroTech 가 안 쓰는 Nextra 규칙 27개를 제거(50547→47371B, 3176↓). dist CSS 의 'nextra' 문자열 0개.
- **대상:** `.nextra-button/callout/card(+변형)/cards/copy-icon/scrollbar(+::-webkit pseudo)/steps/container` 컴포넌트 규칙 + `html[data-nextra-word-wrap] pre`(코드 줄바꿈 토글, 코드블록 없어 미사용).
- **안전 제거 방식:** styles.css 가 minified(한 줄)라 정규식 일괄은 위험 → **CSS 규칙 단위 파서**(중괄호·문자열·`@media` 인식)로 처리. 규칙 선택자에서 `:not(...)`(제외목록)만 무시하고 `nextra` 가 **실제 대상**인 규칙만 제거 → prose 의 `:not()` 제외목록은 보존. (초기엔 `:is()`까지 벗겨 2개를 놓쳐, `:not()` 만 벗기도록 수정.)
- **검증:** dist HTML 의 `.nextra-` 사용 0 확인(완전 dead). 중괄호 균형(467/467). `go test ./...` 통과. 홈·2g 스크린샷 픽셀 동일.

### Phase 7 정리 — Nextra 네이밍 제거 (#__next→#app, nx-*→rt-*)

- **Summary:** 페이지 셸의 `<div id="__next">` 를 `<div id="app">` 로, CSS·템플릿의 `nx-*` 접두사를 `rt-*`(RetroTech)로 전역 치환해 Nextra 흔적 제거. 순수 클래스명 변경이라 렌더는 동일.
- **안전성 사전 검증:** styles.css 가 minified(한 줄 50KB)라 전역 치환만 가능. `nx-` 522곳이 전부 클래스 문맥(`.nx-`·`\:nx-`·`[class~=nx-…]`·`-nx-`·`!nx-`)임을 확인(`content:`/`url()`/커스텀속성에 없음). `#__next` 는 styles.css 미참조(0)라 안전. 기존 `rt-` 16곳은 `--tw-prose-invert-*`(커스텀 속성)라 충돌 없음.
- **Changes:** `public/styles.css`(522), `internal/builder/render.go`·`render_layout.go`·`render_assets.go`(53) — `nx-`→`rt-`. `render_layout.go` — `id="__next"`→`id="app"`. `docs/TODO.md` Phase 7 갱신.
- **검증:** `go vet`·`go test ./...` 통과. dist 에 `__next`·`nx-` 0개. 라이트/다크 2g 스크린샷 픽셀 동일(다크 선택자 `.dark .rt-*` 정상). CSS 해시 갱신(c95bf428).
- **함정:** Go 문자열의 `id=\"__next\"` 는 이스케이프 따옴표라 첫 패턴이 안 맞아 식별자(`__next`)만 재치환.

### Phase 7 정리 — 404 페이지에 RetroTech 로고(cover) 추가

- **Summary:** 테마형 404 를 유지하면서, 페이지 상단에 홈과 같은 RetroTech cover 이미지를 추가(길 잃은 방문자를 위해 홈으로 링크). 사용자 요청.
- **Changes:** `internal/builder/render.go`(`Build404Page`) — `<a href="/"><img …cover.svg…></a>` + 기존 `<h1>404: Page Not Found</h1>`.
- **검증:** 빌드 후 `/404.html` 스크린샷 — cover(홈처럼) + 404 메시지 + 푸터 정상 렌더 확인. `go test ./...` 통과.

### Phase 7 정리 — `scripts/convert` 삭제 (죽은 1회성 도구)

- **Summary:** mdx→md 1회성 마이그레이션 도구 `scripts/convert/main.go` 제거. 입력 `pages/` 가 Phase H 에서 삭제돼 더는 동작하지 않음. 변환 방식은 worklog·plan 에 기록되어 있음.
- **Changes:** `scripts/convert/` 삭제. `docs/ARCHITECURE.md`(디렉터리 트리·제약 항목에서 제거), `docs/TODO.md`(Phase 7 항목 완료 표시).
- **Tests:** `go build ./...`·`go test ./...` 통과(빌드 경로와 무관).

### Phase 7 정리 — 피드 `<generator>` 정확히 (RSS for Node → RetroTech)

- **Summary:** 옛 rss 라이브러리 잔재인 `<generator>RSS for Node</generator>` 를 `RetroTech` 로 교체(부정확한 메타데이터 정정). 골든 갱신, 아이템 계약 불변.
- **Changes:** `internal/builder/feed.go`(generator 값 + 상단 주석), `testdata/feed.golden.xml`.
- **Tests:** `go test ./...` 통과(피드 골든). guid/enclosure/pubDate 등 구독자 계약 불변.

### cmd/serve 빈 포트 자동 선택 (8080 충돌 회피)

- **Summary:** 미리보기 서버가 8080 고정 대신 **빈 포트를 자동 선택**하도록 변경. 8081부터 첫 빈 포트를 잡고(8080 은 다른 프로젝트에 양보), 다 막혔으면 OS 임의 할당. `PORT` 로 강제 지정 가능.
- **Context:** 사용자가 다른 프로젝트에서 8080 을 동시에 사용 중이라 충돌. (앞서 "좀비 서버"로 본 것도 사실은 그 프로젝트의 8080 응답 — `/` 200·`/episodes/2g` 404 가 그 증거였다.)
- **Changes:** `cmd/serve/main.go` — `net.Listen` 으로 8081~8130 첫 빈 포트 탐색(실패 시 `localhost:0`), 실제 포트를 출력. `http.ListenAndServe`→`http.Serve(ln,...)`. README·QUALITY_GATE 의 8080 언급 갱신.
- **검증:** 바이너리 직접 실행 → **8081 선택 확인**(8080 미사용), 라우트 / · /episodes/2g · /feed.xml = 200, 없는 경로 = 404. `go vet`·`go test ./...` 통과. 테스트 서버는 직접 종료(좀비 없음).

### cmd/serve 404 핸들러 정리

- **Summary:** 미리보기 서버의 404 처리가 `w.WriteHeader(404)` 후 `http.ServeFile` 을 호출해 "superfluous response.WriteHeader" 경고를 로그에 남기던 것을, 404.html 을 직접 읽어 404 상태로 쓰도록 수정.
- **Context:** 백그라운드로 띄운 미리보기 서버 로그에서 경고 발견. (참고: 이 환경의 백그라운드 `go run` 은 래퍼가 reaping 되며 컴파일된 바이너리를 orphan 으로 남겨 8080 점유가 꼬일 수 있음 — 안정적 미리보기는 사용자 터미널에서 `go run ./cmd/serve` 권장.)
- **Changes:** `cmd/serve/main.go` — 404 경로에서 `os.ReadFile` + `Content-Type` + `WriteHeader(404)` + `Write`.
- **Tests:** `go vet`·`go test ./...` 통과. 서버 라우트 수동 확인(/, /episodes, /episodes/2g=200, 없는 경로=404).

### badges 폴백 동작 확인 + 워크플로 문서화

- **Summary:** 프론트매터 `badges` 가 없거나 일부만 있을 때의 동작을 확인하고(필드별로 비면 쇼/채널 링크로 폴백 — 옛 `Badges.tsx` 기본값 그대로), 발행 워크플로를 문서화했다.
- **Context:** 사용자 질문 — 새 에피소드는 발행 후 몇 시간 뒤에야 각 플랫폼에 등록되어 딥링크를 알 수 있어, 처음엔 값 없이 쇼 링크로 두었다가 나중에 채우는 운영 방식. 지금도 그렇게 되는지 확인 요청.
- **확인:** `RenderBadges` 의 `orDefault(b.Apple, defaultApple)` 가 **필드별 독립 폴백**. ①badges 없음→전부 쇼/채널 링크(Apple show·YouTube playlist·Spotify show·RSS), ②부분(예: Apple만)→그 필드만 딥링크·나머지 쇼, ③전부→전부 딥링크. 데모로 3경우 렌더 확인.
- **Changes(문서):** `docs/ARCHITECURE.md` 프론트매터 `badges` 주석에 폴백·발행 워크플로 명시. `README.md` 새 에피소드 추가에 "발행 직후 비워둬도 쇼 링크로 연결, 등록되면 필드별로 채움" 단계 추가.
- **Tests:** 코드 변경 없음(기존 `badges_test.go` 가 기본값/토글 검증). `go test ./...` 통과.
- **Docs:** ARCHITECURE, README, worklog.

### 레퍼런스 블록을 순수 마크다운으로 (raw `<div class="refs">` 제거)

- **Summary:** 콘텐츠의 raw `<div class="refs">` 래퍼를 제거하고, 빌더가 `#### 레퍼런스:` 헤딩 뒤 리스트를 자동으로 `.refs` 로 감싸게 했다. 본문은 순수 마크다운, 스타일은 동일.
- **Context:** 사용자 지적 — 마크다운에 raw HTML(`<div class="refs">` + 빈 줄 트릭)이 섞여 어색. `<!--badges-->` 와 같은 방식(빌더가 표현 담당).
- **Changes:**
  - `internal/builder/render.go` — `wrapReferences`(`<h4>레퍼런스:</h4>` 뒤 `<ul>` 을 `<div class="refs">` 로 래핑), `renderEpisodeBody` 파이프라인에 추가(decorateHeadings 이전, bare `<h4>` 매칭).
  - `content/episodes/*.md`(22개) — `<div class="refs">`/`</div>` 줄 제거 → `#### 레퍼런스:` + 마크다운 리스트. **본문 div 줄만 정확히 제거**(전역 개행 정리는 description2 의 빈 줄을 건드려 피드를 바꿨기에 철회 → 피드 골든이 잡음).
  - `internal/builder/render_test.go` — 레퍼런스 자동 래핑 회귀 테스트.
  - 문서(ARCHITECURE/README/DESIGN) — 작성 규약을 "헤딩 + 마크다운 리스트(빌더가 .refs 적용)" 로 갱신.
- **검증:** `go test ./...` 통과(피드 골든 포함 — 프론트매터 불변 확인). 렌더 결과 22편 모두 `.refs` 동일. 라이브 대조 텍스트 25/25·링크 24/25 유지(변동 없음). 
- **Quality Gate:** `go vet`·`go test` 통과.
- **교훈:** 콘텐츠 일괄 치환 시 전역 정규식(`\n{3,}`)이 프론트매터까지 건드릴 수 있음 — 피드 골든 테스트가 1바이트 변경을 즉시 검출.

### 전체 페이지 라이브 대조 + episodes 내비 버그 수정

- **Summary:** 로컬 빌드(26페이지)를 운영 사이트와 전부 대조(본문 텍스트 + 모든 링크/이미지)해 `/episodes` 내비에서 홈 링크 누락 버그를 발견·수정하고 회귀 테스트를 추가했다.
- **Context:** 사용자 요청 — 페이지가 적으니 로컬↔라이브 전 페이지 diff. 라이브 HTML 의 Next 프레임워크 잔재(스크립트·`__NEXT_DATA__`·preload)는 노이즈라, article 본문의 **보이는 텍스트 + 링크/이미지**를 추출해 비교(누락·변형 탐지에 가장 확실).
- **발견:**
  - **버그:** `/episodes` 메타 내비가 테마는 `<span>Episodes</span><a href="/">RetroTech</a>`(현재+홈 링크)인데 내 것은 `Episodes` 만 — 홈 링크 누락. → 수정.
  - 텍스트 1글자 차이(전 에피소드)는 라이브의 `<!-- -->`(React 빈 주석)가 텍스트 추출 시 공백이 된 **아티팩트**(렌더는 동일). 주석 제거 후 25/25 일치.
  - `/episodes/1e` 레퍼런스 링크 `http://`(내 것) vs `https://`(라이브): 원본 `1e.mdx`(커밋 76d4135)도 `http://` 확인 → **소스에 충실**(라이브가 Nextra 렌더 중 업그레이드했거나 stale). 내 버그 아님. blogspot 은 http→https 자동 리다이렉트.
  - `/404`: 라이브는 Next 기본 에러 페이지(미테마), 내 것은 사이트 테마 404 — 의도된 개선.
- **Changes:**
  - `internal/builder/render.go` — `BuildEpisodesPage` 내비에 `<a href="/">RetroTech</a>` 추가.
  - `internal/builder/render_test.go` 신규 — 홈/episodes 내비 링크, 에피소드 title·badges 치환·footer 회귀 테스트.
- **검증:** 수정 후 재대조 — **텍스트 25/25, 링크/이미지 24/25 일치**(남은 1건은 1e http/https, 소스 충실). `go test ./...` 통과.
- **Quality Gate:** `go vet`·`go test` 통과.
- **남은 결정(사용자):** 1e 링크를 https 로 올릴지(소스 편집), 404 를 테마형(현행)으로 둘지.

### 마이그레이션 후속 — 피드 description 개선 + GO_VERSION 불필요 확인 + 후속 정리 기록

- **Summary:** 사용자 리뷰 피드백 반영 — ①팟캐스트 채널 `<description>` 을 실제 설명으로 교체, ②`GO_VERSION` 불필요 확인·문서 반영, ③후속 정리 항목을 TODO Phase 7 에 기록.
- **Context:** 마이그레이션 완료 후 사용자 질문 5건 중 조치가 필요한 것들.
  - **#3 피드 description:** 팟캐스트 앱(Apple/Spotify)이 채널 `<description>` 을 표시하는데, 옛 `gen-rss.js` 가 description 미설정→rss 라이브러리가 **제목으로 폴백**해서 설명이 "RetroTech 팟캐스트"(제목 중복)였다. 홈 소개 문구(사이트 자체 설명)로 교체. 아이템 계약(guid/enclosure/pubDate)은 불변.
  - **#1 GO_VERSION:** 같은 패턴의 `blog.outsider.ne.kr` 가 `GO_VERSION` 미설정으로 Cloudflare Pages 에서 빌드됨을 확인 → Cloudflare 가 `go.mod` 에서 Go 버전 자동 감지. 요구사항에서 제거(빌드 실패 시에만 고정).
- **Changes:**
  - `internal/builder/feed.go` — `feedDesc` 상수 추가, 채널 `<description>` 에 사용(이전 `feedTitle` 폴백 제거).
  - `internal/builder/testdata/feed.golden.xml` — 채널 description 라인 갱신(의도된 변경).
  - `docs/DEPLOYMENT.md`·`scripts/cf-build.sh` — `GO_VERSION` "필요"→"보통 불필요(go.mod 자동 감지)".
  - `docs/TODO.md` — **Phase 7(후속 정리 보류)** 추가: Nextra 잔재 네이밍, 홈 본문 마크다운화, `scripts/convert` 제거 검토, 피드 `<generator>` 문자열, 비가시 차이(조치 불필요).
- **답변만(조치 없음):** #2 pubDate 09:00 UTC 는 운영 배포 피드와 일치(잘못된 것 아님). #4 운영 피드 순서가 stale 한 이유 = 커밋 62ac24f(결정적 정렬) 이전 배포라 옛 비결정 순서, 재배포 시 정정(guid 안정→영향 없음).
- **Tests:** `go test ./...` 통과(피드 골든 새 description 으로). `go run ./cmd/build` 성공, 아이템 계약 불변 확인.
- **Quality Gate:** `go vet`·`go test` 통과.
- **Docs:** DEPLOYMENT, TODO, worklog. (메모리에 후속 정리 보류 기록.)

### Phase H — Next/Nextra/npm 제거 + 문서 정리 (마이그레이션 완료)

- **Summary:** Next/Nextra/React/npm 관련 파일을 모두 제거하고, 문서를 Go 기준으로 갱신했다. 로컬 미리보기용 `cmd/serve` 추가. 마이그레이션 완료.
- **Changes:**
  - 제거(41개): `pages/`(원본 `*.mdx` 포함)·`components/`·`styles/`·`theme.config.js`·`next.config.js`·`next-env.d.ts`·`package.json`·`package-lock.json`·`tsconfig.json`·`vitest.config.ts`·`scripts/gen-rss.js`·`gen-rss.test.js`·`__snapshots__`. `node_modules`·`.next` 물리 삭제.
  - `cmd/serve/main.go` 신규 — `dist/` 미리보기 서버(확장자 없는 URL 을 `.html` 로 해석, Cloudflare 와 동일).
  - `internal/builder/feed_test.go` — 피드 골든 소스 경로 `pages/episodes`→`content/episodes`(이관 후), 통과 확인.
  - `.gitignore` — Go 기준으로 정리(npm/Next 항목 제거).
  - 문서: `README.md`·`docs/ARCHITECURE.md`(전면 재작성)·`docs/DESIGN.md`(기술 결정)·`docs/QUALITY_GATE.md`(Go 명령)·`docs/TESTS.md`(Go 테스트) 갱신. `plan/go-static-migration.md` 상태 완료, `TODO.md` Phase 6 완료 체크.
  - `public/feed.xml`(옛 npm 빌드 잔재) 삭제 — Go 빌드는 `dist/feed.xml` 만 생성.
- **Tests:** `gofmt -l`(미포맷 0)·`go build`·`go vet`·`go test ./...`(parser/builder) 통과. `go run ./cmd/build` 성공(26 HTML + feed.xml + `assets/styles.<hash>.css`). 홈/2g/다크/`episodes` 스크린샷 정상.
- **Quality Gate:** 전 항목 통과.
- **Docs:** README, ARCHITECURE, DESIGN, QUALITY_GATE, TESTS, plan, TODO, worklog.
- **마이그레이션 결과:** 외부 의존성 `goldmark`+`yaml.v3` 2개(이전 수백 개 npm), 브라우저 프레임워크 JS 0(이전 ~104KB), 시각·동작·피드 동일.
- **남은 논의(사용자 확인):** 별도 정리.

### Phase G — 빌드/배포/CI 전환

- **Summary:** 스타일시트를 콘텐츠 해시 경로로 옮기고(`/assets/styles.<hash>.css`), Cloudflare 빌드 래퍼·CI·배포 문서를 Go 기준으로 전환했다.
- **Context:** 배포는 기존 Cloudflare Pages git 연동을 유지(텔레그램 알림 보존)하되 빌드만 Go 로. GitHub Actions+wrangler 로 바꾸는 것보다 변경 최소.
- **Changes:**
  - `cmd/build/main.go` — `fingerprintStylesheet`: `dist/styles.css`→`dist/assets/styles.<sha8>.css` 이동, 페이지가 해시 href 참조. `Site.StylesheetHref` 추가(빈 값은 `/styles.css` 폴백).
  - `public/_headers` — `/_next/static/*`(이제 무의미) 제거, `/assets/*` 1년 immutable 추가.
  - `scripts/cf-build.sh` — `npm run build`→`go run ./cmd/build`. `GO_VERSION`·`ANALYTICS_ID` 안내 추가.
  - `.github/workflows/ci.yml` — Node→Go: `go vet`·`go test`·`go run ./cmd/build`.
  - `docs/DEPLOYMENT.md` — 빌드 명령(Go)·출력·`GO_VERSION`(필요)·`ANALYTICS_ID`(프로덕션 GA), 캐시(`/assets/*`), CI(Go) 갱신.
- **사용자 작업 필요(배포 시):** Cloudflare Pages 대시보드에 `GO_VERSION=1.26.2`, 프로덕션에 `ANALYTICS_ID=G-PVJ12C7HR6` 설정.
- **Tests:** `go test ./...` 통과. `go run ./cmd/build` 성공, `dist/assets/styles.8af44033.css` 생성·페이지 참조·해시경로 200·옛 `/styles.css` 404 확인. 스크린샷 재확인(스타일 유지).
- **Quality Gate:** `go vet`·`go test` 통과.
- **Next:** Phase H — Next/Nextra/npm 제거 + 잔여 문서(ARCHITECURE/DESIGN/QUALITY_GATE/TESTS/README) 갱신.

### Phase C–F — 페이지 생성 + 콘텐츠 변환 + 시각 패리티 검증

- **Summary:** Go로 홈/에피소드/목록/404 HTML과 badges를 생성하는 렌더러를 만들고, 23편 `.mdx`를 `content/episodes/*.md`로 변환한 뒤, 참고 빌드와 **스크린샷 비교로 시각 패리티(라이트/다크)** 를 확인했다.
- **Context:** HTML은 피드와 달리 Next 런타임(React/JSON/preload)을 걷어내는 게 목적이라 바이트 동일이 아니라 **시각·동작 동일**이 목표. 재사용한 테마 CSS(`db4552…css` → `public/styles.css`)가 `nx-*` 클래스를 스타일링하므로 같은 DOM+같은 CSS=동일 렌더.
- **Changes:**
  - `internal/builder/badges.go` — `RenderBadges`(Badges.tsx 마크업 재현, google↔rss 토글, href 이스케이프).
  - `internal/builder/render.go` — 페이지 빌더(`BuildHomePage`/`BuildEpisodesPage`/`BuildEpisodePage`/`Build404Page`), goldmark(GFM+Unsafe), 후처리(외부링크 `target=_blank`+sr-only, heading `subheading-h{n}`+anchor, `<!--badges-->`치환), post-item 목록.
  - `internal/builder/render_layout.go` — `pageShell`(article 안에 footer 배치 — 테마와 동일), `headHTML`(meta/og/favicon/CSS/분석 마커), 메타블록, footer, GA 주입, 날짜(isoDate/displayDate).
  - `internal/builder/render_assets.go` — footer 인라인 스타일, 다크모드 부트스트랩/토글 스크립트(next-themes 대체, 프레임워크 0), 해/달 아이콘 스왑 CSS, 소셜/RSS SVG.
  - `cmd/build/main.go` — dist 청소 → public 복사 → 페이지/피드 생성. `ANALYTICS_ID` 환경변수로 GA 주입(배포만).
  - `scripts/convert/main.go` — 1회성 마이그레이션 도구. 프론트매터 **원문 보존**(피드 패리티) + `<Badges>`→`badges:` 추출, 본문 title h1 제거·`<!--badges-->`마커·refs div CommonMark화.
  - `internal/parser` — `Episode.ContentHTML`→`Body`(렌더는 builder 책임), goldmark 의존성 제거.
  - `public/styles.css` — 테마+main.css 컴파일본 재사용(50KB).
  - `content/episodes/*.md` — 23편 변환.
- **검증(스크린샷 비교, 1100×1400):** 홈·2g(라이트)·2g(다크) 모두 참고와 픽셀 단위 일치. footer를 article 안으로 이동(nx-prose 너비), 다크 토글 해→달 스왑 추가. 잔존 검사: dist에 `<!--badges-->`/`className`/`<Badges>` 0, 23편 모두 badges div.
- **피드 회귀:** content 기반 `dist/feed.xml`이 골든과 여전히 동일(변환기가 프론트매터 보존).
- **Tests:** `go test ./...` 통과(parser/builder, badges 3 + feed 골든). `go run ./cmd/build` 성공(23편+홈/목록/404/피드, ~35ms).
- **Quality Gate:** `go vet ./...`·`go test ./...` 통과.
- **알려진 미세차(비가시/허용):** h1 후행 `\n` 없음, dateTime 속성 UTC(표시는 동일), `'`↔`&#x27;`(둘 다 `'` 렌더), next/image 내부 속성(data-nimg 등) 생략.
- **Next:** Phase G — 빌드/배포/CI 전환. Phase H — Next 제거·문서.

### Phase B — RSS 피드 바이트 패리티 (feed.go + 골든)

- **Summary:** `internal/builder/feed.go` 로 iTunes 팟캐스트 RSS 피드를 문자열 빌드해 현 `scripts/gen-rss.js`(rss npm 라이브러리) 출력과 **바이트 단위로 동일**하게 재현하고, 골든 테스트를 추가했다.
- **Context:** "지금과 똑같이"의 최우선 리스크 = 구독자 계약(피드). `encoding/xml`은 CDATA·네임스페이스 순서·self-closing 규칙을 그대로 못 내므로 문자열로 정확히 재현.
- **핵심 패리티 처리:**
  - `guid`/`enclosure`/`link`/title(폴드 trailing `\n`)/description(+description2 `\n` 연결)/duration/itunes 필드 모두 일치.
  - **pubDate = `09:00 GMT` (UTC 고정).** 운영 배포 피드가 09:00 GMT(Cloudflare UTC 빌드)라 거기에 맞춤 → 빌드 머신 TZ와 무관하게 결정적(기존 TZ 의존 비결정성 제거). 로컬 KST 빌드는 00:00 GMT였으나 구독자가 받는 건 운영(09:00)이라 그 기준 채택.
  - rss 라이브러리 산출물(generator="RSS for Node", 채널 `description`=title)까지 그대로 재현 → 최종 논의 목록(정리 여부).
  - 정렬: 엄격한 날짜 내림차순(동일 날짜 id 내림차순). 운영 피드는 커밋 62ac24f(결정적 정렬) 이전이라 순서가 다른 stale 상태 — 재배포 시 자동 정정(guid 안정이라 구독자 영향 없음).
- **버그 수정:** 날짜가 0-미패딩(`2024/9/18`)이라 파싱 레이아웃 `2006/01/02`(2자리 강제)가 단일자릿수에서 실패→정렬 깨짐. `2006/1/2`(1~2자리 허용)로 수정.
- **Changes:**
  - `internal/builder/feed.go` — `BuildFeed`(정렬·채널·아이템), `feedDescription`·`pubDate`·`enclosureType`·`cdata`.
  - `internal/builder/feed_test.go` + `testdata/feed.golden.xml`(운영 기준 09:00) — 23개 항목 바이트 골든(lastBuildDate 정규화).
  - `internal/parser/parser.go` — `SortEpisodes` 추출(피드에서 재사용), `dateLayout` 0-미패딩 허용으로 수정.
- **Tests:** `go test ./internal/...` 통과(parser 6 + builder 골든). 운영 피드와 교차검증: 내용 동일, 순서만 stale 차이 확인.
- **Quality Gate:** `go test ./internal/...` 통과.
- **Docs:** worklog.
- **Next:** Phase C — Badges HTML 렌더 + 본문 변환 규칙(마커) 확정.

### Phase A — Go 스캐폴딩 + 파서 + 참고 빌드 캡처

- **Summary:** Go 모듈 초기화(`go.mod`, goldmark+yaml.v3 2개 의존성)와 `internal/parser`(프론트매터 분리·YAML 파싱·goldmark 렌더·에피소드 로드/정렬)를 구현하고 단위 테스트를 추가했다. 또한 현 Next 사이트를 빌드해 정답지(`_ref_dist/`)를 캡처했다.
- **Context:** Go 마이그레이션(Phase 6) 착수. "빌드 결과물이 지금과 똑같이" 가 최우선 기준이라, 먼저 현 `dist/`를 `_ref_dist/`로 캡처해 feed.xml·HTML·컴파일된 CSS·파일 경로의 기준점으로 삼았다.
- **참고 캡처에서 확정한 사실:**
  - Next 정적 익스포트는 **평면 `.html`**(`episodes/2g.html`, `episodes.html`, `index.html`, `404.html`) — Go도 동일 경로로 emit.
  - 테마 CSS는 단일 파일 `_next/static/css/db4552ef5233e278.css` → 재사용 가능.
  - feed.xml(31622B)의 pubDate가 `00:00:00 GMT`(KST 빌드, 09:00 KST=00:00 UTC) → 결정적 재현 가능.
- **Changes:**
  - `go.mod`/`go.sum` — module `github.com/outsideris/retrotech`, go 1.26.2.
  - `internal/parser/parser.go` — `Frontmatter`(title/date/description/description2/author/enclosure/duration/badges)·`Episode`·`SplitFrontmatterAndBody`·`ParseFrontmatter`·`RenderMarkdown`(goldmark `WithUnsafe`)·`LoadEpisodes`(index 제외·날짜 내림차순·동일 날짜 id 내림차순).
  - `internal/parser/parser_test.go` — 프론트매터 분리, 폴드/따옴표 title의 trailing newline 보존(피드 패리티 핵심), 블록 스칼라 description, refs div raw HTML 통과, 로드 정렬·index 제외.
  - `.gitignore` — `/_ref_dist/`, Go 바이너리/`*.out`.
- **Tests:** `go test ./internal/parser/` 통과(6 테스트). `npm ci && npm run build`(참고 빌드) exit 0.
- **Quality Gate:** Go 기준 도입 전 단계. `go test ./internal/parser` 통과.
- **Docs:** worklog. (QUALITY_GATE/ARCHITECURE는 Phase H에서 일괄 갱신.)
- **Next:** Phase B — `feed.go`(encoding 문자열 빌드)로 현 feed.xml 바이트 패리티 + 골든 테스트.

### Go 정적 생성기 마이그레이션 계획 수립

- **Summary:** Nextra/Next 의존성을 제거하고 자체 제작 Go 정적 생성기로 전환하는 마이그레이션 계획을 수립했다. `docs/plan/go-static-migration.md`(ADR+Phase) 작성, TODO Phase 6 추가.
- **Context:** 사용자 요구 — "Nextra 제거 + 외부 의존성 최소화 + 지금과 똑같은 형태". 논의 결과: ①오픈소스 SSG(Astro/Eleventy)도 배제하고 자체 제작, ②언어는 Go(longevity·표준 라이브러리·기존 검증 패턴), ③범위는 빌드/프레임워크 의존성만(GA4·Sponsors 유지).
- **Changes:**
  - `docs/plan/go-static-migration.md` 신규 — 배경/결정(ADR), 불변식(URL·feed 가입자 안정성·똑같은 형태·자산 경로), 목표 아키텍처(goldmark+yaml.v3 2개 의존성, cmd/internal/templates/content 구조), MDX→MD 변환 규칙, Phase A~H, 품질 게이트(Go), 위험/병행·롤백.
  - `docs/TODO.md` — Phase 6(A~H) 추가. 기존 "Next 업그레이드 검토"를 본 마이그레이션으로 대체 표시.
- **참고 조사:** `blog.outsider.ne.kr`(같은 저자의 Go 정적 블로그 빌더) 코어 파이프라인 분석 — `internal/parser`(프론트매터+goldmark), `internal/builder`(html/template), `encoding/xml` 직접 RSS, 다크모드 인라인 스크립트, `ANALYTICS_ID` GA 주입, GitHub Actions→`wrangler pages deploy` 배포. 코드 하이라이팅·OG 이미지 생성·페이지네이션 등은 RetroTech 불필요로 제외.
- **현 RSS 계약 확정:** `scripts/gen-rss.js` 분석 — guid=`/episodes/{id}`, pubDate=`{date} 09:00`, enclosure, itunes 필드, 날짜 내림차순(동일 날짜 name 내림차순). Phase B 골든 테스트 기준.
- **Tests:** 없음(계획 문서 단계, 코드 변경 없음).
- **Quality Gate:** 해당 없음(문서만). 코드 착수는 Phase A부터.
- **Docs:** `docs/plan/go-static-migration.md`(신규), `docs/TODO.md`.
- **Next:** 사용자 확인 후 Phase A(스캐폴딩) 착수. 오픈 결정: Badges 콘텐츠 표현 방식(D1), 배포 시크릿(D2), "똑같은 형태" 허용 오차(R3).

## 2026-06-15

### RSS 검증 자동화 (포맷 스냅샷 + 데이터 유효성 + CI)

- **Summary:** RSS의 의도치 않은 변경을 잡기 위해 ①포맷 스냅샷 ②실제 에피소드 데이터 유효성 테스트를 추가하고, ③GitHub Actions CI(push/PR 에서 test+build)를 도입했다.
- **Context:** 사용자 요청 — "의도치 않은 RSS 변경을 CI에서 검증". 검증 대상은 포맷+데이터 둘 다, 실행은 GitHub Actions 로 선택.
- **Changes:**
  - `scripts/gen-rss.js` — 순수 `buildFeedXml`(피드 XML 생성)·`readEpisodes`(파일 읽기)를 분리·export(출력 동일). `feedOption` 을 모듈 스코프로 이동.
  - `scripts/gen-rss.test.js` — 포맷 스냅샷(고정 픽스처, 특수문자 CDATA escape·최신순 포함) + 실제 에피소드 데이터 유효성(전 회차 title/date/duration/description/`.mp3`·항목 수). `scripts/__snapshots__/gen-rss.test.js.snap` 커밋.
  - `.github/workflows/ci.yml` — Node 20, `npm ci && npm test && npm run build`, push(main)/PR 트리거.
- **Tests:** `npm test` 16개 통과(13→16). 스냅샷 비교 통과(written 아님). `npm run build` 통과. gen-rss 리팩터 출력 동일(23개·최신순) 확인.
- **Quality Gate:** `npm run build` + `npm test` 통과. QUALITY_GATE 에 CI 명시.
- **Docs:** TESTS.md, QUALITY_GATE.md, worklog.
- **Next:** 첫 push 후 GitHub Actions 실행 결과 확인. 의도된 포맷 변경 시 `npx vitest -u` 로 스냅샷 갱신.

## 2026-06-14

### RSS 항목 순서 결정적 정렬 (최신순)

- **Summary:** `gen-rss.js` 의 피드 항목 순서를 발행일 내림차순(최신순)으로 **결정적** 정렬하도록 고쳤다. 기존엔 `Promise.all` 동시 읽기 중 `feed.item()` 을 호출해 빌드마다 순서가 랜덤이었다(6f13544 이전부터 있던 레이스).
- **Context:** 사용자 요청. 먼저 검증: 6f13544 이후 RSS 형식·데이터는 불변(항목 23개 내용·채널 헤더 바이트 동일), 순서만 비결정적임을 옛/새 gen-rss 출력 비교로 확인. 순서 변경은 guid(=permalink)·pubDate 가 그대로라 구독자에 영향 없음(클라이언트는 guid 로 식별, pubDate 로 정렬).
- **Changes:** `scripts/gen-rss.js` — 항목을 모은 뒤(`Promise.all` 은 입력 순서 보존) `sortByDateDesc`(date desc, 동일 날짜는 name desc 타이브레이크, 입력 배열 불변)로 정렬해 `feed.item()` 호출. `sortByDateDesc` export. `scripts/gen-rss.test.js` — 정렬 테스트 3개 추가(최신순·결정적·타이브레이크·불변).
- **Tests:** `npm test` 13개 통과. gen-rss 2회 실행 → 순서 동일(결정적)·최신순(2g→…→0. Pilot) 확인. `npm run build` 통과. 항목 23개·내용 불변.
- **Quality Gate:** `npm run build` + `npm test` 통과.
- **Docs:** ARCHITECURE RSS 설명, TESTS.md 갱신.
- **Next:** 없음.

### 푸터 아이콘 레이아웃 깨짐 수정 (svg display:block 회귀)

- **Summary:** FontAwesome→인라인 SVG 교체(bb07687) 후 푸터 소셜 아이콘이 각자 한 줄을 차지하며 깨진 것을 수정. `.social-icon`/`.rss-icon` 에 `display: inline-block` 추가.
- **Context:** nextra-theme-blog 의 Tailwind preflight 에 `img,svg,video,…{display:block}` 이 있어, 인라인이던 FA `<i>` 와 달리 SVG 가 block 이 되어 "아이콘 / 링크"가 줄바꿈됨. 클래스 selector 라 specificity 로 테마 element 규칙을 덮어씀.
- **Changes:** `styles/main.css` — `.social-icon`/`.rss-icon` 에 `display: inline-block`.
- **Tests:** `npm run build` 성공, 로컬 빌드 푸터 스크린샷으로 아이콘이 링크와 한 줄에 정렬됨 확인. `npm test` 통과.
- **Quality Gate:** `npm run build` + `npm test` 통과.
- **Docs:** worklog.
- **Next:** 원래 푸터는 가운데 정렬이 아니라 아바타 `float:left` 레이아웃. 원하면 센터링/정돈 별도 가능.

### 배포 알림 엔드포인트 정정 (/webhook/generic)

- **Summary:** cf-webhook 워커 코드를 확인해, 빌드 래퍼의 커스텀 페이로드는 `/webhook/cloudflare`(네이티브 CF 알림용)가 아니라 **`/webhook/generic`** 으로 보내야 함을 확인하고 `cf-build.sh`·문서를 정정했다.
- **Context:** 사용자 질문 — 워커가 받는 URL에 맞는지. 워커(`~/Dropbox/projects/vibe-coding/cf-webhook`) 소스 확인: `/webhook/cloudflare` 어댑터는 CF 네이티브 페이로드(`data.status`/`text`/`alert_type`)를 파싱 → 커스텀 `{status,…}` 최상위 키는 `unknown`·빈 값으로 깨짐. `/webhook/generic`(`parseGeneric`)이 `status`(`failure`→`failed`)·`project`·`branch`·`commitSha`·`url`·`message` 를 최상위에서 읽음. 빈 필드는 워커가 생략.
- **Changes:** `scripts/cf-build.sh` — 안내 URL 을 generic 으로, 페이로드를 `branch`/`commitSha`/`url` 구조화 필드로 정정(상태 이모지·라벨은 워커가 부여하므로 message 에서 제거). `docs/DEPLOYMENT.md` — `DEPLOY_WEBHOOK_URL`=`/webhook/generic`, 페이로드/대안(네이티브는 `/webhook/cloudflare`) 정정. `docs/TODO.md`.
- **Tests:** 페이로드 JSON 유효성 확인, `cf-build.sh` 로컬 실행(미설정 시 스킵·exit 0). `npm test` 통과.
- **Quality Gate:** `npm run build`(래퍼 내부) 통과.
- **Docs:** DEPLOYMENT.md, TODO.
- **Next (사용자):** 대시보드 `DEPLOY_WEBHOOK_URL` 을 `…/webhook/generic?token=…` 로 설정(스모크 테스트도 generic 으로).

### 배포 성공/실패 텔레그램 알림 (빌드 래퍼)

- **Summary:** Cloudflare Pages 배포 결과를 사용자가 운영 중인 텔레그램 Worker(`cf-webhook.outsideris.workers.dev`)로 통지하는 빌드 래퍼 `scripts/cf-build.sh` 를 추가했다.
- **Context:** Worker 가 커스텀 페이로드(`{status,project,environment,message}`)를 받는다. Cloudflare 네이티브 Notifications 웹훅은 자체 스키마(`name/text/alert_type/…`)라 맞지 않아, **빌드 종료 코드(0=성공)** 로 성공/실패를 분기해 Worker 로 POST 하는 방식 채택.
- **Changes:** `scripts/cf-build.sh` 신규(`npm run build` → 종료코드별 success/failure POST → 빌드 코드로 종료, 웹훅 실패는 무시). `docs/DEPLOYMENT.md` 알림·빌드 설정 갱신, `docs/TODO.md` Phase 4.
- **Tests:** 래퍼 로컬 실행 — `DEPLOY_WEBHOOK_URL` 미설정 시 빌드 성공·알림 스킵·exit 0 확인. 실패 분기 로직 확인. (`npm test` 영향 없음, 통과.)
- **Quality Gate:** `npm run build`(래퍼 내부) 통과.
- **Docs:** DEPLOYMENT.md, TODO.
- **Next (사용자 작업):** Cloudflare Pages 대시보드에서 Build command=`bash scripts/cf-build.sh`, 환경변수 `DEPLOY_WEBHOOK_URL`(암호화) 설정 → 첫 배포로 success 알림 확인.

### 테스트 도입 (Vitest): gen-rss + Badges

- **Summary:** Vitest 기반 테스트를 도입하고(`npm test`), `gen-rss.js` 의 프론트매터→RSS 변환 로직과 `Badges` 컴포넌트 분기를 검증하는 테스트 10개를 추가했다.
- **Context:** 사용자 요청(가장 의미 있는 작업). 프로젝트에 자동화 테스트가 없던 공백 해소.
- **Changes:**
  - 의존성: `vitest`·`@vitejs/plugin-react`·`@testing-library/react`·`jsdom`(devDeps), `package.json` 에 `test` 스크립트.
  - `scripts/gen-rss.js` 리팩터 — 순수 로직 `episodeToItem`/`shouldSkip` 분리·export, `generate()` 는 `require.main===module` 가드(테스트 import 부작용 제거). 동작 동일(빌드 시 feed.xml 23항목 정상).
  - `vitest.config.ts`(react 플러그인; node 기본, 컴포넌트는 jsdom), `scripts/gen-rss.test.js`(6), `components/Badges.test.tsx`(4; next/image·link mock).
  - `tsconfig.json` `exclude` 에 `**/*.test.ts(x)` + `vitest.config.ts` 추가 → `next build` 타입체크에서 제외(plugin-react 가 구식 moduleResolution 으로 타입 못 찾던 문제 회피).
- **Tests:** `npm test` → 2파일·10테스트 통과. `npm run build` 통과(타입체크 OK).
- **Quality Gate:** `npm run build` + `npm test` 통과. QUALITY_GATE.md 에 테스트 항목 추가.
- **Docs:** TESTS.md 재작성, QUALITY_GATE/TODO/ARCHITECURE 갱신.
- **Next:** (선택) 피드 생성 end-to-end 테스트.

### 빠른 정리: feed.xml gitignore + 배지 SVGO

- **Summary:** 빌드 산출물 `public/feed.xml` 을 `.gitignore` 에 추가하고, 배지 SVG 5개를 SVGO 로 최적화(총 ~80KB→35KB).
- **Context:** 사용자 요청(빠른 정리 묶음).
- **Changes:** `.gitignore`(`/public/feed.xml`), `public/badges/{apple,google,rss,spotify,youtube}.svg`(SVGO, viewBox 보존, 기본 정밀도).
- **Tests:** `npm run build` 성공. 배지 4개(에피소드 페이지) + google.svg 직접 렌더 스크린샷으로 시각 동일 확인.
- **Quality Gate:** `npm run build` 통과.
- **Docs:** TODO Phase 1/4 갱신.
- **Next:** 테스트 도입(gen-rss, Badges).

### GTM 제거, GA4 직접 로드만 유지

- **Summary:** 분석을 GA4(gtag.js) 직접 로드만 남기고 Google Tag Manager 컨테이너를 제거했다(사용자 결정: GA4만 직접).
- **Context:** 이전엔 GTM 컨테이너(`GTM-P368DQ3M`)와 GA4 gtag(`G-PVJ12C7HR6`)를 둘 다 로드 → 미사용 JS 중복 + (GTM 안에 GA4 태그가 있었다면) 이중 집계 위험. 1인 정적 사이트라 GA4 직접만으로 충분.
- **Changes:** `pages/_app.tsx`(GTM `<Script>` 로더 제거, GA4 gtag 2개 유지), `pages/_document.tsx`(GTM `<noscript>` iframe 제거).
- **Tests:** `npm run build` 성공. dist 전체에서 `GTM-P368DQ3M` 흔적 0개, GA4 `G-PVJ12C7HR6`/`gtag/js` 는 `_app` 청크에 유지 확인. GTM `noscript`(정적 HTML)도 제거 확인.
- **Quality Gate:** `npm run build` 통과.
- **Docs:** TODO Phase 2, PERFORMANCE 요약 갱신.
- **Next:** 배포 후 GA4 실시간 보고서로 수집 정상 확인 권장. GitHub Sponsors iframe 지연은 남은 선택 항목.

### caniuse-lite 갱신 (레거시 JS 검토 결과)

- **Summary:** `npx update-browserslist-db@latest` 로 caniuse-lite 갱신(1.0.30001517→30001799), 빌드의 "caniuse-lite is outdated" 경고 제거. 레거시 JS(12KiB) 자체는 설정으로 줄일 수 없음을 확인.
- **Context:** 사용자 요청(권장 2번, 레거시 JS 축소). 시도: 모던 browserslist + tsconfig target es2017.
- **결과/판단:** browserslist·target 을 바꿔도 framework/main/polyfills 청크 해시가 동일하고 공유 JS 84.4kB 불변 → 레거시 JS 는 Next 내장 청크라 설정으로 안 줄어든다. 모던 browserslist 는 이득 없이 구형 브라우저용 트랜스파일 범위만 좁혀 호환성 위험이 생겨 **되돌렸다.** 효과 있는 caniuse-lite 갱신만 유지.
- **Changes:** `package-lock.json`(caniuse-lite 버전).
- **Tests:** `npm run build` 성공, browserslist/caniuse 경고 없음.
- **Quality Gate:** `npm run build` 통과.
- **Docs:** TODO Phase 4, PERFORMANCE 빌드 산출물 노트.
- **Next:** 레거시 JS 는 Next 업그레이드 시 재검토. 분석(GTM/GA)은 사용자와 논의(아래 설명).

### 배지 이미지 preload 경쟁 제거 (LCP)

- **Summary:** 배포 후 PageSpeed(모바일 성능 89) 검토 중, 배지 이미지 4~5개가 모두 `priority` 라 히어로와 함께 high-priority 프리로드되어 LCP와 대역폭을 경쟁하던 것을 발견. `Badges.tsx` 의 모든 `<Image>` 에서 `priority` 제거.
- **Context:** 사용자 요청(권장 1번). 배지는 히어로 아래(스크롤 영역)라 LCP 무관 → 프리로드 불필요. 히어로(cover.svg)의 `priority` 는 유지.
- **Changes:** `components/Badges.tsx` — 5개 Image 의 `priority` 제거.
- **Tests:** `npm run build` 성공. 빌드 HTML 의 이미지 프리로드가 히어로(cover.svg) 하나만 남고 배지 4개 프리로드 제거 확인.
- **Quality Gate:** `npm run build` 통과.
- **Docs:** TODO Phase 1, PERFORMANCE #5 갱신.
- **Next:** 레거시 JS 검토, 분석(GTM/GA) 설명.

### outsider.png → 240px WebP (110KB→5KB)

- **Summary:** 푸터 프로필 이미지(859×859 PNG, 120px 로 표시)를 240px WebP 로 교체했다. 110KB→5KB(95%↓).
- **Context:** 사용자 요청(개선 차례 4/4). 표시 크기 대비 원본이 과대했음.
- **Changes:** `public/images/outsider.webp` 신규(`cwebp -q 82 -resize 240 240`), `public/images/outsider.png` 제거, `theme.config.js`(src→webp, width/height=120 지정).
- **Tests:** `npm run build` 성공. dist 에 webp(5KB) 확인·png 없음. 푸터 스크린샷으로 아바타 정상 렌더 확인.
- **Quality Gate:** `npm run build` 통과.
- **Docs:** TODO Phase 1, PERFORMANCE 표 갱신.
- **Next:** 요청한 4개 개선(접근성·CLS·FontAwesome·이미지) 모두 완료. 배포 후 운영 재측정 권장.

### FontAwesome 킷 제거 → 인라인 SVG 아이콘

- **Summary:** 푸터 아이콘 4개(twitter/github/blog/rss)를 인라인 SVG 로 대체하고 외부 FontAwesome Pro 킷 스크립트를 제거했다. 전 페이지에서 외부 요청 ~9개 + 웹폰트 3개 절감.
- **Context:** 사용자 요청(개선 차례 3/4). 아이콘 4개를 위해 Pro 킷 전체(kit.js + CSS 5 + woff2 3)를 모든 페이지에서 로드하던 문제.
- **Changes:** `components/Icons.tsx` 신규(FA Free 6.7.2 path, CC BY 4.0, currentColor·1em), `theme.config.js`(아이콘 컴포넌트 사용), `pages/_document.tsx`(kit 스크립트 제거), `styles/main.css`(.social-icon/.rss-icon 정렬).
- **Tests:** `npm run build` 성공. 빌드 HTML 에 `fontawesome`/`fa-` 없음 확인, 인라인 SVG 4개 삽입 확인. 푸터 스크린샷으로 아이콘 정상 렌더 확인.
- **Quality Gate:** `npm run build` 통과.
- **Docs:** TODO Phase 2, PERFORMANCE 요약 갱신.
- **Next:** outsider.png 리사이즈+WebP.

### CLS 제거 (히어로 이미지 비율 예약)

- **Summary:** 홈 히어로 이미지를 `width={0} height={0}` → `width={3000} height={3000}` + `style height:auto` 로 바꿔 1:1 비율 공간을 예약했다. 간헐 CLS 0.25 → 0.
- **Context:** 사용자 요청(개선 차례 2/4). cover.svg viewBox 3000×3000(정사각형).
- **Changes:** `pages/index.mdx`.
- **Tests:** `npm run build` 성공. 빌드 HTML img 에 `width="3000" height="3000" ... height:auto` 확인. 모바일 Slow4G 트레이스 CLS 0.00. 스크린샷으로 히어로 정상 렌더 확인.
- **Quality Gate:** `npm run build` 통과.
- **Docs:** TODO Phase 1 CLS 완료.
- **Next:** FontAwesome 서브셋.

### 접근성 100 달성 (link-name + main 랜드마크)

- **Summary:** 푸터 RSS 아이콘 링크에 `aria-label`, 장식 아이콘에 `aria-hidden`, 콘텐츠 `<article>` 에 `role="main"`(라우트 변경 시 재적용)을 적용했다. Lighthouse 접근성 94→100, Agentic 25→100.
- **Context:** 사용자 요청(개선 차례 1/4). nextra-theme-blog 가 `<main>` 을 렌더하지 않아 `_app.tsx` 에서 article 에 role 부여.
- **Changes:** `theme.config.js`(aria-label/aria-hidden), `pages/_app.tsx`(useEffect 로 role=main, asPath 의존).
- **Tests:** `npm run build` 성공 → 로컬 서빙 → Lighthouse 모바일 A11y/BP/SEO/Agentic 모두 100, 실패 0.
- **Quality Gate:** `npm run build` 통과.
- **Docs:** TODO Phase 3 완료.
- **Next:** CLS(히어로 이미지 높이 예약).

### cover.svg 최적화 + 정적 자산 캐시(_headers) 적용

- **Summary:** `cover.svg` 를 SVGO 로 64.5% 줄이고(402→143KB), `/_next/static/*` 영구 캐시용 `public/_headers` 를 추가했다.
- **Context:** 사용자 요청 — cover.svg 경량화 + 정적 자산 캐시 연장(최대 ROI). Cloudflare Pages `_headers` 는 대시보드 설정 없이 적용되고 기본값을 override 함(공식 문서 확인).
- **Changes:**
  - `public/images/cover.svg` — SVGO(precision 2, viewBox 보존, multipass). 402,012→142,707 bytes, `<path>` 436→306 병합. 원본/최적화 렌더 스크린샷 비교로 **시각 동일** 확인.
  - `public/_headers` 신규 — `/_next/static/*` → `Cache-Control: public, max-age=31536000, immutable`.
- **Tests:** `npm run build` 성공. `dist/_headers` 생성·내용 확인, `dist/images/cover.svg` = 142,707 bytes 확인. 브라우저 렌더 비교(원본=최적화).
- **Quality Gate:** `npm run build` 통과.
- **Docs:** PERFORMANCE.md(적용 현황·표 갱신), TODO.md(2개 항목 완료), ARCHITECURE.md·DEPLOYMENT.md(`_headers` 명시).
- **Next:** 배포 후 `curl -I https://retrotech.outsider.dev/_next/static/...` 로 `immutable` 반영 확인. outsider.png WebP·FontAwesome 서브셋·접근성(aria-label/`<main>`)·CLS 는 미적용.

### cover.svg 영향 정정 및 캐시 룰 위치 보강

- **Summary:** "cover.svg 다운로드 4ms" 가 재방문 캐시 적중 값이었음을 정정하고(콜드 첫 방문은 brotli 118KB), Cloudflare Cache Rule 설정 위치를 PERFORMANCE.md 에 보강했다.
- **Context:** 사용자 질문 — cover.svg 용량이 괜찮은지 + 캐시 룰을 어디서 설정하는지. 확인 결과 cover.svg 는 436개 `<path>` 벡터 일러스트(임베디드 비트맵 아님), raw 402KB / brotli 118KB, 좌표 정밀도 소수점 6자리라 SVGO 로 축소 여지가 크다.
- **Changes:** `docs/PERFORMANCE.md` — LCP 분해/자산 표/요약/개선 항목에서 4ms(캐시 적중) 오해 정정, 콜드 첫 방문 영향 명시, 캐시 룰 대시보드 경로(Caching → Cache Rules / Configuration → Browser Cache TTL) 추가.
- **Tests:** 측정/문서(코드 변경 없음). `curl` 로 cover.svg 압축 크기(brotli 118KB) 실측.
- **Quality Gate:** 코드 변경 없음.
- **Docs:** 위 문서.
- **Next:** 없음.

### 운영 사이트 PageSpeed/성능 재측정 (Cloudflare)

- **Summary:** 운영 사이트(https://retrotech.outsider.dev)를 모바일 Slow 4G / CPU 4×로 재측정하고, 성능·접근성 개선점을 PERFORMANCE.md·TODO.md 에 갱신했다.
- **Context:** 사용자 요청 — page speed 검사 + 성능/접근성 개선점. 이전 측정은 로컬 빌드(`http.server`) 기준이라 호스팅(압축/캐시/CDN)이 빠져 있었음.
- **결과:** LCP 1,022ms(양호; load delay 439ms가 최대 몫), CLS 0.00(트레이스)/0.25(Lighthouse, 간헐), A11y 94 / BP 100 / SEO 100. 호스팅: Brotli·HTTP/3 ON. **캐시 문제 발견** — `/_next/static/*` 해시 자산도 4시간(`max-age=14400`)뿐(1년 immutable 권장). LCP 요소는 `cover.svg` 지만 brotli+h3 로 전송 4ms라 원본 크기는 LCP 병목 아님.
- **개선 우선순위:** ①정적 자산 1년 캐시 ②FontAwesome 서브셋 ③접근성(aria-label/`<main>`) ④히어로 CLS ⑤LCP preload ⑥이미지 다이어트 ⑦서드파티 지연.
- **Changes:** `docs/PERFORMANCE.md` 운영 기준 전면 갱신, `docs/TODO.md` 캐시 항목(최대 ROI) 추가·우선순위 조정.
- **Tests:** 측정만(코드 변경 없음).
- **Quality Gate:** 코드 변경 없음.
- **Docs:** 위 문서.
- **Next:** TODO Phase 2(캐시) → Phase 1(CLS/이미지) 순 권장.

### 배포(Cloudflare) 및 텔레그램 알림 옵션 문서화

- **Summary:** 배포가 Cloudflare로 이뤄짐을 문서에 명시하고, 배포 성공 시 텔레그램 알림을 받는 두 가지 방법(빌드 명령 curl / Notification 웹훅 + Worker 중계)을 정리했다.
- **Context:** 사용자 요청 — Cloudflare 배포 명시 + "배포 성공 시 텔레그램 알림 가능?". GitHub Actions 등 별도 CI 없음. 배포 세부 연결 방식은 사용자가 별도 관리(문서엔 코드 기준 빌드 설정만 명시), 알림은 "옵션만 문서화" 선택. Cloudflare 알림 전송 수단은 이메일/웹훅/PagerDuty 뿐이라 텔레그램은 중간 변환 필요함을 검색으로 확인.
- **Changes:** `docs/DEPLOYMENT.md` 신규(배포 개요·빌드 설정·텔레그램 알림 옵션 A/B·봇 준비·참고 링크). `docs/ARCHITECURE.md` 배포 섹션을 요약+링크로 갱신. `README.md` 배포 안내 및 문서 표에 DEPLOYMENT 추가. `docs/TODO.md` 선택 항목 추가.
- **Tests:** 문서 변경(코드 영향 없음).
- **Quality Gate:** 코드 변경 없음.
- **Docs:** 위 문서.
- **Next:** 알림을 실제로 적용하려면 DEPLOYMENT.md 의 옵션 A 또는 B 선택.

### howler 의존성 잔여 문서 정리

- **Summary:** 세션 중 `package.json`/`package-lock.json` 의 미커밋 변경(=`howler` 추가)이 사라진 것을 추적한 결과, Player WIP 정리의 일부로 `howler` 가 제거되었음을 확인하고 문서의 howler 잔여 서술을 정리했다.
- **Context:** 시작 시 `M package.json`/`M package-lock.json` 의 정체는 `howler` 추가였다(Player.tsx 와 한 묶음의 옛 WIP). 사용자가 Player.tsx 삭제와 함께 howler 도 되돌려, 현재 `howler` 는 package.json 에 없고 코드에서도 미사용. 점검 결과 내 3개 커밋 어디에도 package.json 이 포함되지 않았고(git show), git 훅은 100MB 검사·커밋 메시지 템플릿뿐이라 파일을 되돌리지 않으며, stash·오늘자 dangling 객체도 없음 → 내 작업이 아니라 사용자의 정리로 확인.
- **Changes:** `docs/ARCHITECURE.md` 스택 표의 howler 행과 주의사항 항목 제거, `docs/TODO.md` Phase 4 의 howler 제거-검토 항목 제거, `docs/TESTS.md` 외부 의존성 예시에서 howler 제거.
- **Tests:** 문서 변경만(코드 영향 없음). `grep -rn howler pages components` → 사용처 없음 확인.
- **Quality Gate:** 코드 변경 없음.
- **Docs:** 위 문서 갱신.
- **Next:** 없음.

### 파비콘 / PWA 아이콘 적용

- **Summary:** 예전에 추가만 해두고 `<head>` 에 연결하지 않았던 `public/favicon_io/` 아이콘 세트를 실제로 적용했다.
- **Context:** 사용자 질문 — favicon_io 를 지금 추가해도 유효한지. 형식은 모두 표준이나 코드에서 참조되지 않았고(빌드 HTML에 아이콘 `<link>` 없음), 매니페스트의 `name`/`short_name` 이 비어 있고 아이콘 경로가 루트인데 파일은 `/favicon_io/` 에 있어 불일치 상태였다.
- **Changes:**
  - `public/favicon_io/*` → `public/` 루트로 이동(매니페스트의 루트 경로와 일치시킴, `favicon_io/` 제거).
  - `public/site.webmanifest`: `name="RetroTech 팟캐스트"`, `short_name="RetroTech"` 채움.
  - `pages/_document.tsx` `<Head>` 에 favicon(16/32)·apple-touch-icon·manifest `<link>` 5개 추가.
- **Tests:** `npm run build` 성공. 빌드 HTML에 `<link>` 5개 삽입 확인, 참조 아이콘 7개 모두 `dist/` 에 존재, 에피소드 페이지에도 전역 적용 확인.
- **Quality Gate:** `npm run build` 통과.
- **Docs:** `docs/ARCHITECURE.md` 정적 자산 트리 및 `_document.tsx` 설명 갱신.
- **Next:** (선택) android-chrome-512x512.png 281KB 는 PWA 설치 시에만 로드되어 페이지 성능엔 무관. 필요시 최적화.

### Player.tsx 잔재 관련 문서 정리

- **Summary:** 옛 작업의 잔재였던 `components/Player.tsx` 를 사용자가 삭제함에 따라, 그 파일을 전제로 들어갔던 서술(빌드 차단 이슈, 인앱 플레이어 WIP 등)을 문서에서 제거하고 빌드 상태 기술을 정정했다.
- **Context:** 직전 커밋(c655078)은 Player.tsx 가 빌드를 깨뜨린다고 기록했으나, 해당 파일은 미사용 잔재로 사용자가 삭제. 이제 `npm run build` 가 정상 통과한다.
- **Changes:** ARCHITECURE/DESIGN/PERFORMANCE/TODO/QUALITY_GATE/TESTS/README/worklog 에서 Player.tsx·인앱 플레이어 관련 내용 제거.
- **Tests:** `npm run build` 성공 확인(정적 HTML 27p 익스포트).
- **Quality Gate:** `npm run build` 통과.
- **Docs:** 위 문서들 갱신.
- **Next:** docs/TODO.md Phase 1(이미지·CLS) 부터 개선 권장.

### 성능 검사 및 설계·구성 문서화

- **Summary:** 현재 구성으로 정적 사이트를 빌드해 성능을 측정하고, 코드를 다시 읽지 않고도 참고할 수 있도록 설계/구성/성능 문서 일습을 작성했다.
- **Context:** 사용자 요청 — "현재 구성에서 빌드된 사이트의 성능을 검사하고, 이후 작업 시 참고할 설계·구성 문서를 작성".
- **Changes (조사/측정, 코드 변경 없음):**
  - `npm run build` 로 정적 사이트를 빌드(통과)한 뒤 측정.
  - `dist/` 를 `python3 -m http.server` 로 서빙, Chrome DevTools 성능 트레이스 + Lighthouse 측정.
  - 빌드가 생성한 `public/feed.xml`(미추적·비-gitignore)은 세션 시작 상태로 되돌리기 위해 제거.
- **측정 결과(요약, 상세는 docs/PERFORMANCE.md):**
  - 정적 HTML 27p, `dist/` 3.7 MB. 홈 First Load JS ~104 kB.
  - 최대 이슈: `cover.svg` 402 KB(gzip 153 KB) 히어로, FontAwesome Pro 킷 풀로드(외부 ~9요청)+GitHub Sponsors iframe+GTM/GA 전 페이지 부하, 히어로 이미지 높이 미예약으로 Lighthouse 모바일 **CLS 0.25**.
  - Lighthouse(모바일): A11y 94 / BP 100 / SEO 100 / Agentic 25. `link-name`·`landmark-one-main` 실패.
  - `next/image`(akamai 로더)는 이 구성에서 최적화하지 않음(passthrough).
  - 한계: 로컬 서빙이라 TTFB≈1ms·무압축·무캐시 → 로딩 지표는 낙관적, 호스트 설정은 미검증.
- **Tests:** 자동화 테스트 없음(러너 부재). `npm run build` 로 타입체크/익스포트 확인(통과).
- **Tests Added:** 없음(이번 작업은 조사·문서화).
- **Quality Gate:** `npm run build` 성공.
- **Docs:** 신규 — `docs/ARCHITECURE.md`, `docs/DESIGN.md`, `docs/PERFORMANCE.md`, `docs/TODO.md`, `docs/QUALITY_GATE.md`, `docs/TESTS.md`, 본 worklog. `README.md` 보강.
- **Next:** docs/TODO.md Phase 1(이미지·CLS) 부터 개선 권장.
