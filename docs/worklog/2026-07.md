# 작업 로그 — 2026-07

## 2026-07-27 02:10 - RetroTech Editor 브랜치(claude/great-bardeen-bc5022)를 main 에 머지

- Summary: 로컬에만 있던 에피소드 에디터 데스크톱 앱 작업(Phase 8, 19커밋, 2026-06-21~27)을 main 에 머지했다. 머지 안 된 채 잊혀 소스가 로컬 브랜치에만 존재하던 상태를 해소.
- Context: 사용자 요청 — 브랜치 정체·미머지 사유 조사 후 머지 승인. 조사 결과 머지 보류 결정 기록 없음(작업 산출물이 설치된 `.app` 이라 소스 머지가 누락된 것). `git merge-tree` 로 무충돌 확인 후 진행.
- Changes:
  - 머지 유입: `internal/editor`(스토어·합성기·초안·assist), `cmd/app`(사이드카+`injectLoginPath`), `desktop/`(Electron+electron-builder), 에피소드 프론트매터 1회성 정규화(값·피드 불변), docs(TODO Phase 8·TESTS·QUALITY_GATE·DESIGN·plan/episode-editor-app.md·worklog 2026-06).
  - 머지 후 수정: `content/episodes/2h.md` 에서 `author: Outsider` 제거 — 브랜치가 author 필드를 드랍하고 빌더에 하드코딩(4d310d0)했는데, 브랜치 이후 main 에서 추가된 2h 가 옛 스타일이라 에디터 라운드트립 테스트(`TestRoundTripPreservesEveryEpisode/2h`)가 실패했다. dc:creator 는 하드코딩이라 피드 출력 불변.
- Tests: `go build ./...`·`go vet ./...`·`go test ./...` 전체 통과(에디터 패키지 포함). `go run ./cmd/build` 성공(24편), `dist/feed.xml` 이 커밋된 골든과 `lastBuildDate` 외 바이트 동일 확인.
- Tests Added: 없음(브랜치의 에디터 테스트들이 머지로 유입 — TESTS.md 는 브랜치에서 이미 갱신됨).
- Quality Gate: 컴파일/정적검사·테스트·빌드·RSS·정적 산출물 확인. Lighthouse 는 CI `lighthouse` 잡에서 검증(사이트 마크업 무변경 — 에디터는 `cmd/build` 산출물에 미포함).
- Docs: 이 worklog. 나머지 문서는 브랜치에서 이미 갱신된 채 머지됨.
- Next: push 후 CI 확인. (후속 TODO: 미리보기 raw 섹션 구조화·회차 복제·초안 git 동기화 — TODO.md Phase 8 참고)

## 2026-07-27 01:50 - 에피소드 2h 추가로 깨진 피드 골든 갱신 (CI 수정)

- Summary: 커밋 69e557d("episode 2h")가 `content/episodes/2h.md`만 추가하고 피드 골든을 갱신하지 않아 CI의 `TestBuildFeedMatchesGolden`이 실패했다. 골든을 재생성해 CI를 복구했다.
- Context: push 후 GitHub Actions `test` 잡 실패(run 30210808446). 실제 피드의 첫 `<item>`은 새 에피소드 `2h. VCS: Subversion`인데 골든은 여전히 `2g. VCS: SourceForge`가 첫 항목이었다.
- Changes:
  - `internal/builder/testdata/feed.golden.xml` — `go run ./cmd/build` 산출물(`dist/feed.xml`)로 재생성. diff 확인 결과 새 2h `<item>` 추가와 `lastBuildDate`(테스트에서 정규화됨) 외 기존 항목의 guid/enclosure/pubDate 변경 없음.
  - `docs/TESTS.md` — 새 에피소드 추가 시 골든 갱신 절차 명문화(빌드 → 복사 → diff 검토 → 에피소드 md와 같은 커밋). 골든 검증 대상을 "23편"에서 "전체 에피소드"로 일반화.
  - `docs/QUALITY_GATE.md` — 테스트 항목에 "새 에피소드 추가 시 골든 동시 갱신" 리마인더 추가, 정적 산출물 개수 하드코딩(26개) 제거.
- Tests: `go build ./...`, `go vet ./...`, `go test ./...` 모두 통과. `go run ./cmd/build` 성공(24 에피소드, HTML 27개 + feed.xml + sitemap.xml + fingerprinted CSS 확인).
- Tests Added: 없음(테스트 데이터인 골든 파일만 갱신).
- Quality Gate: 컴파일/정적검사·테스트·빌드·RSS 생성·정적 산출물 확인 완료. Lighthouse CI(`npx @lhci/cli autorun`)는 로컬 미실행 — CI `lighthouse` 잡에서 검증 예정(변경이 피드 XML과 문서뿐이라 페이지 마크업 영향 없음).
- Docs: `docs/TESTS.md`, `docs/QUALITY_GATE.md`.
- Next: push 후 CI 통과 확인.
