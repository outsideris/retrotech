# 작업 로그 — 2026-09

## 2026-09-20

### 20:26 — Assist 사이드바의 모델·effort 선택을 현재 CLI 기준으로 갱신

- Summary: 데스크톱 에디터 Assist 사이드바의 모델/effort 드롭다운을 설치된 claude·codex CLI 의 현재 모델로 교체하고, effort 를 provider 단위가 아니라 **모델 단위**로 고르게 했다. 어휘의 원본을 `assist/catalog.go` 한 곳으로 옮겨 `/api/assist/providers` 가 내보내고 UI 가 그대로 그린다.
- Context: 사이드바 목록이 낡아 있었다 — claude 는 별칭(`haiku`/`sonnet`/`opus`)만, codex 는 계정 카탈로그에서 사라진 `gpt-5`·`gpt-5-codex` 와 더 이상 없는 effort(`none`/`minimal`)를 제시했다. 모델마다 effort 상한이 다른데 UI 는 provider 하나에 목록 하나만 가질 수 있었다.
- Changes:
  - `internal/editor/assist/catalog.go`(신규): `Model{ID,Label,Efforts}` 와 provider 별 목록, `Validate`/`validateOptions`. claude = Opus 5·Sonnet 5·Fable 5.1·Haiku 4.5(effort 없음), codex = GPT-5.6 Sol·Terra(ultra 까지)·Luna(max 까지)·GPT-5.5(xhigh 까지). 모델 id 는 별칭이 아니라 정식 id 를 쓴다.
  - `internal/editor/assist/assist.go`: `Provider` 에 `Models()` 추가, `claudeEfforts`/`codexEfforts` 닫힌 집합 삭제 → `validateOptions` 로 교체(이제 model 도 검증한다). gemini 의 `Run` 도 같은 검증을 거쳐 "모든 `Run` 은 검증을 통과한 옵션만 받는다"가 성립한다. `Options` 주석 갱신.
  - `internal/editor/editor.go`: `/assist/providers` 가 모델 목록까지 내보낸다(노브 없는 CLI 는 `[]`). `run`·`analyze` 가 `assist.Validate` 로 CLI 실행 전에 400 을 낸다.
  - `internal/editor/assets/app.js`: `ASSIST_TUNING` 표 삭제 → 서버 카탈로그(`assistModels`). `renderTuning`/신규 `renderEfforts`/`unionEfforts`, `fillSelect` 는 `{value,label}` 를 받고 저장값이 더 이상 유효하지 않으면 `(기본)` 으로 떨어진다. 모델을 바꾸면 effort 목록을 다시 그린다.
  - `internal/editor/assets/index.html`: effort 라벨에 `id="assist-effort-knob"` — effort 를 안 받는 모델에서 그 노브만 숨긴다.
- Tests: `go build ./...`, `go vet ./...`, `go test ./...`, `go test -race ./internal/editor/... ./internal/research`, `go run ./cmd/build`(24편 + feed/sitemap), `cd desktop && npm test` 모두 통과.
- Tests Added: `internal/editor/assist/catalog_test.go`(카탈로그 형식·**전체 카탈로그 핀**(id·label·effort 목록까지 `reflect.DeepEqual`)·`Validate` 수용/거부 표·가짜 CLI 로 "거부가 exec 전에 일어남" 확인), `internal/editor/editor_test.go`(providers 페이로드의 `models`·gemini 의 `[]` 직렬화, run/analyze 의 모델·effort 400 + PATH 를 가짜 CLI 로 격리해 실제 CLI 미실행 확인, `index.html`↔`app.js` 의 effort 노브 id 계약).
- Quality Gate: 필수 항목(컴파일·정적검사·테스트·빌드·RSS/정적 산출물) 확인. 공개 사이트 코드·콘텐츠·자산 변경이 없어 Lighthouse 재측정은 면제 조건을 적용했다(에디터 UI 는 `cmd/build` 에 포함되지 않는다). GUI 는 자동 검증 대상이 아니라 `go run ./cmd/app` + 실제 브라우저로 확인했다: 모델 목록, GPT-5.5 선택 시 effort 가 xhigh 까지로 줄고 Sol 에서 ultra 가 돌아옴, Haiku 4.5 에서 effort 노브가 사라짐, gemini 에서 줄 전체가 사라짐, Sol 에서 ultra 를 고른 뒤 GPT-5.5 로 가면 `(기본)` 으로 떨어지고 Sol 로 돌아오면 ultra 가 복원됨.
- Verify: 변경 직후 다중 에이전트 리뷰를 돌려 세 가지를 잡아 고쳤다. ① 처음 쓴 "CLI 가 실행되지 않았음" 가드가 **헛돌고 있었다** — `t.Setenv("PATH", binDir)` 뒤에는 `/bin/sh` 가 `touch` 를 찾지 못해 흔적 파일이 절대 생기지 않는다(셸 리다이렉션 `: >` 로 교체). ② HTTP 검증 테스트에 PATH 격리가 없어, 검증이 빠지면 테스트가 진짜 CLI 에 돈을 쓸 수 있었다(가짜 CLI 로 격리). ③ 모델 목록 핀이 id 만 비교해 label·effort 변조를 못 잡았다(전체 비교로 교체). 두 가드는 **변이 테스트로 실제로 실패하는지 확인**했다 — 검증 코드를 지우면 "an AI CLI was executed" 로 떨어진다. CLI 계약을 실제 호출로 확인했다 — `claude` Haiku 4.5(effort 없음)와 `codex` GPT-5.6 Terra/low 가 사이드바와 같은 경로로 응답했고, 은퇴한 `gpt-5-codex` 는 400 으로 끊겼다. 조사 과정에서 두 가지를 바로잡았다. ① **GPT-6 Astra 는 카탈로그에 넣지 않았다** — 계정 카탈로그의 최신 모델이지만 설치된 codex 0.146.0 으로는 서버가 `The 'gpt-6-astra' model requires a newer version of Codex` 로 거절한다(codex 업그레이드 후 Sol 과 같은 low…ultra 로 추가하면 된다). ② **claude CLI 는 effort 를 검증하지 않는다** — Haiku 4.5 에 `--effort` 를 줘도 오류 없이 조용히 버린다. 그래서 "CLI 가 거절하겠지"에 기댈 수 없고 카탈로그가 직접 알고 있어야 한다.
- Docs: `docs/plan/episode-editor-app.md`(튜닝·API·UI), `docs/ARCHITECURE.md`(Assist 항목), `docs/TESTS.md`(catalog_test 행 신설·assist/editor 행 갱신), `README.md`, `docs/QUALITY_GATE.md`(마지막 검토).
- Next: codex 를 올리면 Astra 를 카탈로그에 추가한다. 사이드바와 달리 `cmd/research` 리더의 모델 목록은 의도적으로 좁은 큐레이션(Astra 제외, effort 는 medium/high/xhigh)이라 이번 변경에서 건드리지 않았다 — `docs/RESEARCH_WORKBENCH.md` 의 결정이 그대로 유효하다.

## 2026-09-16

### 22:34 — Add an anchored local research reader

- Summary: HTML의 읽기 경로에 문단별 채팅·원자료 조사·표시된 추가/정정 문단을 연결하는 로컬 리더를 구현했다.
- Context: 긴 채팅에서 주 스토리라인을 다시 찾는 문제와, 후속 질문으로 드러난 누락을 다음 주제 조사에 재사용하려는 요청.
- Changes: `cmd/research`, `internal/research`의 원문 보존·앵커·상태/HTTP·렌더·스킬 규칙 반영, `internal/editor/assist/research.go`의 로컬 CLI 구조화 조사, 임베드 채팅 UI와 실행기. 기본 Sol/high, 선택한 대화의 부모 이력을 명시적으로 재사용. 실행 바이너리는 사이트 빌드가 정리하는 dist 밖 `.local-bin`에 둔다.
- Tests: `go build ./...`, `go vet ./...`, `go test ./...`, `go test -race ./internal/research ./internal/editor/assist`, `go run ./cmd/build` 통과. 처음 전체 테스트는 샌드박스의 httptest 포트 제한으로 실패했고 로컬 포트가 허용된 실행에서 전수 통과했다. 공개 사이트 24편·RSS/iTunes·홈/목록/404·sitemap·CSS 확인.
- Tests Added: `internal/research/research_test.go`(삽입·원문/상태 보존·오류·출처·HTTP·취소·명시적 스킬 반영·선택한 대화 맥락), `internal/editor/assist/research_test.go`(CLI 계약/구조화 결과/오류), `internal/research/browser_test.go` + `scripts/test-research-browser.mjs`(실제 보고서 사본을 쓰는 선택 Chrome 통합 검사).
- Quality Gate: 조사 리더 범위의 필수 검사 통과. 공개 사이트 소스·자산·콘텐츠 변경이 없어 사이트 Lighthouse 재측정 면제 조건을 적용했다. 리더는 별도 Chrome UI 검사로 검증했다.
- Verify: 1차 Go/race 검증에서 상태 파일 쓰기 실패 시 HTML/Markdown 복구와 출처 ID·JSON 끝 검증을 보강. 2차 Chrome 검증에서 문단 선택·비동기 삽입 후 위치 유지·정정/이스케이프·숨김/복원/재열기·스킬 편집/선택·오프라인/인쇄/390px/320px 통과. 3차 최종 Chrome 재검사 및 설치본에서 기존 119문단·14장·101출처·원본 Markdown 불변 확인. 별도 Ralph Loop 세션은 사용하지 않았다.
- CLI Verification: Codex CLI 0.146.0의 ChatGPT 로그인으로 Sol/high 실제 조사 완료. bzr-svn 메타데이터 질문에 당시 커밋 문서 2개를 사용한 결과를 테스트 사본에 저장했다. Claude 2.1.251은 설치됐지만 미로그인으로 실 모델 호출 미검증; CLI envelope/error 계약은 fake 실행 파일로 검증했다.
- Docs: README, ARCHITECURE, DESIGN, TODO, TESTS, QUALITY_GATE, RESEARCH_WORKBENCH 및 상세 계획 갱신. 별도 로컬 RetroTech 스킬에는 상호작용 절차·검토한 후속 규칙의 재사용 지침을 백업 후 반영하고 quick_validate 통과.
- Next: 사용자는 현재 보고서의 start-research.command로 다시 열 수 있다. Claude 사용 시 사용자 로그인 필요. 수동 본문 재편집 후 앵커 재배치·강제 종료 중 파일 불일치 자동 복구는 제공하지 않으며 보존본/상태로 수동 복구한다.
