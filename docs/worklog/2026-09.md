# 작업 로그 — 2026-09

## 2026-09-16

### 22:34 — Add an anchored local research reader

- Summary: HTML의 읽기 경로에 문단별 채팅·원자료 조사·표시된 추가/정정 문단을 연결하는 로컬 리더를 구현했다.
- Context: 긴 채팅에서 주 스토리라인을 다시 찾는 문제와, 후속 질문으로 드러난 누락을 다음 주제 조사에 재사용하려는 요청.
- Changes: `cmd/research`, `internal/research`의 원문 보존·앵커·상태/HTTP·렌더·스킬 규칙 반영, `internal/editor/assist/research.go`의 로컬 CLI 구조화 조사, 임베드 채팅 UI와 실행기. 기본 Sol/high, 선택한 대화의 부모 이력을 명시적으로 재사용. 실행 바이너리는 사이트 빌드가 정리하는 dist 밖 `.local-bin`에 둔다.
- Tests: `go build ./...`, `go vet ./...`, `go test ./...`, `go test -race ./internal/research ./internal/editor/assist`, `go run ./cmd/build` 통과. 처음 전체 테스트는 샌드박스의 httptest 포트 제한으로 실패했고 로컬 포트가 허용된 실행에서 전수 통과했다. 공개 사이트 24편·RSS/iTunes·홈/목록/404·sitemap·CSS 확인.
- Tests Added: `internal/research/research_test.go`(삽입·원문/상태 보존·오류·출처·HTTP·취소·명시적 스킬 반영·선택한 대화 맥락), `internal/editor/assist/research_test.go`(CLI 계약/구조화 결과/오류), `internal/research/browser_test.go` + `scripts/test-research-browser.mjs`(실제 보고서 사본을 쓰는 선택 Chrome 통합 검사).
- Quality Gate: 조사 리더 범위의 필수 검사 통과. 공개 사이트 소스·자산·콘텐츠 변경이 없어 사이트 Lighthouse 재측정 면제 조건을 적용했다. 리더는 별도 Chrome UI 검사로 검증했다.
- Verify: 1차 Go/race 검증에서 상태 파일 쓰기 실패 시 HTML/Markdown 복구와 출처 ID·JSON 끝 검증을 보강. 2차 Chrome 검증에서 문단 선택·비동기 삽입 후 위치 유지·정정/이스케이프·숨김/복원/재열기·스킬 편집/선택·오프라인/인쇄/390px/320px 통과. 3차 최종 Chrome 재검사 및 설치본에서 기존 119문단·14장·101출처·원본 Markdown 불변 확인. 별도 Ralph Loop 세션은 사용하지 않았다.
- CLI Verification: Codex CLI 0.146.0의 ChatGPT 로그인으로 Sol/high 실제 조사 완료. bzr-svn 메타데이터 질문에 당시 커밋 문서 2개를 사용한 결과를 테스트 사본에 저장했다. Claude 2.1.251은 설치됐지만 미로그인으로 실 모델 호출 미검증; CLI envelope/error 계약은 fake 실행 파일로 검증했다.
- Docs: README, ARCHITECURE, DESIGN, TODO, TESTS, QUALITY_GATE, RESEARCH_WORKBENCH 및 상세 계획 갱신. 별도 로컬 RetroTech 스킬에는 상호작용 절차·검토한 후속 규칙의 재사용 지침을 백업 후 반영하고 quick_validate 통과.
- Next: 사용자는 현재 보고서의 start-research.command로 다시 열 수 있다. Claude 사용 시 사용자 로그인 필요. 수동 본문 재편집 후 앵커 재배치·강제 종료 중 파일 불일치 자동 복구는 제공하지 않으며 보존본/상태로 수동 복구한다.
